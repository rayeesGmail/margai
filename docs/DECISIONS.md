# Decisions log

Where docs/SPEC.md and docs/DEV_SPEC.md are silent, CLAUDE.md says: choose the boring, maintainable
option and record it here. One line per decision, newest first. Conflicts between documents are not
recorded here — they are surfaced in the session and resolved in the documents themselves.

Format: `date · day · decision · why · revisit when`

- 2026-09-08 · D7 · **Login by a verified email while the SMS DLT template (F1) is blocked** — `users.email` joins `phone` as a login identifier (either suffices while active; V6 relaxes the V2 check), `otp_challenges` carry `channel` + `destination`, `POST /auth/otp/request` takes `{phone}` or `{email}`, and the SMS channel stays designed in behind `margai.auth.otp.channels` (default `[email]`); SPEC §3/§5 are untouched and remain the target; TECH_PLAN §1.4, §2.2, §2.10, §3.2, §3.4, §3.7, §7.3, §9.6 carry dated in-place amendments; PLAN D8/D11 and the Week-2 gate read "email" in TRACKER · the founder learned at the D7 plan review that DLT registration needs a registered company; an unverified phone plus an emailed code would let anyone claim another person's number, so email had to be the verified identifier (founder decisions 1 and 3, 2026-09-08) · **exit condition:** F1 approval → add `sms` to the channels, the MSG91 adapter (D11) and a phone-attach-by-OTP flow (PARKED); then this row closes.
- 2026-09-08 · D7 · OTP email delivery through **AWS SES v2 over the SDK default credential chain** (`margai.auth.otp.sender = ses`, `email-from` a verified identity, `ses-region`; the SDK's default retries kept, 15-second call timeout; failures surface as `OtpSendException` carrying only the SES error code); the `log` sender is the sandbox everywhere else · no secrets and the same identity story as Bedrock (TECH_PLAN §7.4); SES needs identity verification and production access (TRACKER F10), not a company registration (founder decision 2) · if SES sandbox limits or deliverability argue for another mailer.
- 2026-09-08 · D7 · `Language` and `UserRole` live in `common.api`, and `common.api.Principal(userId, role, language)` is the request principal every module's controllers read; the security layer publishes it as a request attribute so `common` resolves the response language without depending on Spring Security · common owns the envelope's language and the JWT claims; TECH_PLAN §1.4 gives feature modules no edge to `auth` · never.
- 2026-09-08 · D7 · A blank `margai.auth.jwt.secret` or `margai.auth.otp.pepper` becomes a random 256-bit value for the process with a WARN; a configured value under 256 bits refuses to start; `jwt.secret-previous` is accepted while configured, with no timed window (the F8 rotation runbook removes it after §9.2's 15 minutes) · never a fixed dev key in the tree; local works out of the box and tokens and pending codes die on a restart, the right failure there; SSM (F8) sets both in AWS · F8 SSM seeding, or if a timed window is ever needed.
- 2026-09-08 · D7 · Identifier normalisation: Indian mobiles only at beta (ten digits starting 6–9, optional `+91`/`91`/`0`, spacing tolerated) → E.164; emails trimmed, lowercased, RFC-shape, ≤ 254, no provider canonicalisation (Gmail dots and plus tags PARKED); `code_hash = SHA-256(pepper ‖ challenge_id ‖ code)` hex with a constant-time compare · SPEC is India-only; the challenge id salts every hash · when a non-Indian number is ever a login.
- 2026-09-08 · D7 · OTP outcomes: a request inside the 30-second cooldown or over 3 per hour per destination → 429 `OTP_RATE_LIMITED` with `retry_after_s` (no silent re-send); a phone request while `channels = [email]` → 400 `VALIDATION_FAILED` on `phone`; a wrong code → 401 `OTP_INVALID` with `attempts_left` (0 on the fifth); an unknown, used, expired or exhausted challenge → 401 `OTP_EXPIRED`; a delivery failure after the challenge is stored deletes the row, counts `otp.send_failed` and surfaces as 500 `INTERNAL` with the request id · one remedy per code, the hourly cap stays fair, no new §3.3 codes (closing questions 1, 2 and 4 at the D7 approval) · D9's unhappy-path checklist.
- 2026-09-08 · D7 · The sandbox delivery is `LoggingOtpSender` on its own logger `margai.otp.sandbox` (masked destination plus the rendered message, which carries the code); the auth module's own loggers never see a code or a destination (asserted by `OtpServiceTest`) · the log is the inbox until SES or MSG91 delivers; TECH_PLAN §9.1 "codes never logged" binds the service · never.
- 2026-09-08 · D7 · `POST /auth/otp/verify` creates the `users` row at D7 (`language = en` until the mentor intro or `PATCH /me`; `phone_verified_at` stamped on phone logins); the empty `student_profiles` row and `/me` stay D10; `invite_code` is accepted and ignored until D75; `device_label` = `X-App-Version` (else `User-Agent`, ≤ 80) · the JWT `sub` and the `user` payload need the row; PLAN D10 keeps its scope · D10, D75.
- 2026-09-08 · D7 · Refresh rotation is sliding: each refresh issues a successor with a fresh 30-day life in the same family; one family per login/device; reuse of a spent token revokes the family, and that revocation commits although the call fails · TECH_PLAN §3.2 "30 days, rotating" read as "a device that keeps being used stays signed in" (PLAN D10 ✅) · never.
- 2026-09-08 · D7 · Rate limiting with Bucket4j (`bucket4j_jdk17-core`) local buckets, greedy refill on the `IstClock`, Caffeine eviction after an hour idle; keys `user:<id>` for authenticated routes, `ip:<address>` for `POST /auth/otp/request` (10/hour) and `ip-auth:<address>` for the other public `/auth/*` routes (`margai.limits.public-auth-per-ip-per-minute`, 60); the client address is the **last** `X-Forwarded-For` hop (the one the ALB appends in its default `append` mode, so a client cannot choose its bucket by prefixing hops), else the socket address; `Retry-After` in whole seconds rounded up, never 0; `/error` is a permitted route so a failure inside a filter still renders the envelope on Boot's ERROR dispatch; the ArchUnit SDK rule is scoped per service package (Bedrock in `ai.internal.bedrock`, SES in `auth.internal.email`) as §1.4 words it · §3.4 names Bucket4j and no eviction library; §1.5 step 4 says "phone/IP for `/auth/*`"; the first-hop key was a spec-auditor MAJOR (bypassable behind an ALB); the earlier ArchUnit rule banned the whole SDK outside bedrock · second API task (§13.3); F8 must keep the ALB's `xff_header_processing.mode` at `append`.
- 2026-09-08 · D7 · Server copy in `messages/messages_{en,hi,hinglish}.properties` for every `ErrorCode` and message kind (a test enforces completeness in all three languages), every line through MessageFormat (double every apostrophe), Hinglish as its own bundle suffix (`Locale.of("hinglish")`) while the app's locale is `hi_Latn`; the envelope's `details` are machine-readable only — ids, seconds, counts and, for `VALIDATION_FAILED`, field → **reason code** (`not_blank`, `min`, `phone.invalid`, `email.invalid`, `identifier.required`, `identifier.one_only`, `channel.unavailable`, `code.digits`, `body: malformed`, `content_type: unsupported`) with snake_case wire names, which the app renders from its ARB copy — a constraint's reason code is its raw message template when that is a code, else the constraint name, never the interpolated (locale-dependent) text; Jackson snake_case, `non_null` output and lenient input in `application.yml`; `/actuator/health` stays public with component names (the D2 row's "revisit at D7": no change, admin detail is D75) · SPEC §3 "all copy is externalized for EN/HI/Hinglish" and TECH_PLAN §3.1 "never text alone" leave no room for English prose in `details` (spec-auditor MAJOR at D7); TECH_PLAN §3.3, §11.3, §11.7 · the D67 copy pass; D8 adds the ARB entries for the reason codes.
- 2026-09-08 · D6 · Developer-machine Bedrock credentials are documented as "the SDK default chain" — an `aws login` session in the default profile (the SDK `signin` module is on the runtime classpath since D5) or a named profile via `AWS_PROFILE` — in `server/README.md`, the `BedrockSmokeTest` and `BedrockConfiguration` javadocs and the pom comment, while TECH_PLAN §1.2, §7.4 and §7.6 keep naming an IAM Identity Center (SSO) profile as the intended identity · the D5 live smoke ran on an `aws login` session (today the account root user, TRACKER F8) and the docs said "SSO profile", which did not describe the working path; the intended identity is F8's to create, not a documentation fact yet (spec-auditor finding, D6) · when F8 creates the non-root identity: the docs then name that profile and this row closes.
- 2026-09-08 · D5 · `cost_paise` rounds **up** (CEILING), not HALF_UP: any call that consumed tokens costs at least one paisa; a call with no usage stays at zero · the first live smoke (Nova Lite: 29 in / 20 out / 5,976 cached) cost 0.06 and 0.38 paise and rounded to zero, so a stream of tiny calls would never reach the breaker; over-counting by under a paisa per call is the safe direction for SPEC §3's circuit breaker (amends the 2026-09-06 cost row) · if the ledger ever needs sub-paisa precision (schema change: micro-paise).
- 2026-09-08 · D5 · `prices-json` carries a diagnostic row for `apac.amazon.nova-lite-v1:0` (public list prices: 0.06 / 0.24 USD per 1M in / out, cache read 0.006, cache write free, batch at half) so the live smoke can run on an Amazon-owned model while the account's AWS Marketplace subscription for the Anthropic models is blocked (403 INVALID_PAYMENT_INSTRUMENT, AWS ticket open); the tier defaults stay Anthropic and the model is selected per run through `MARGAI_AI_TIER_CHEAP` · proves the Bedrock path — credentials, region, Converse mapping, forced tool, cache accounting, ledger — without touching the contract (SPEC §3 Bedrock) or the tier defaults; a direct Anthropic API fallback was considered and declined for now · when the ticket clears (rerun the smoke on the Anthropic profile; confirm or drop the Nova row).
- 2026-09-06 · D5 · One `ai_calls` row per `AiClient` request: the retry decorator's retries and the schema decorator's repair attempt fold into that row (tokens summed, so the cost is complete; the row reads `ok` when the repair succeeded); the ledger is outermost so breaker refusals and policy errors get rows too; attempts are counted by the `ai.attempts` meter and WARN-logged by the two decorators, and `error_code` stays an error field · TECH_PLAN §4.1 "one AiResponse and one ledger row per request" is how CLAUDE.md's "every Bedrock call logs an `ai_calls` row" is read (spec-auditor finding, D5) · if ops needs per-attempt rows.
- 2026-09-06 · D5 · `AiRequest` carries a ninth component `repair`, set only by the schema-validating decorator; `PromptRef` is name-only — the registry resolves the active version and the ledger stamps it; `Repair`, `AiClientInfo` and `TierPolicyException` live in `ai.api` · the repair turn needs the original request, and a config-driven version cannot live in the caller's reference (TECH_PLAN §4.1 sketch said "name + version") · never.
- 2026-09-06 · D5 · Model-facing fragments shared by every request — the forced tool's description and the repair tool-result message — live in `prompts/_protocol.v<N>.stg`; a `_`-prefixed group file is a fragment group (named templates, no `system`/`user`) rendered through `PromptRegistry.renderFragment` · the ai-layer rule "never inline prompt strings" covers every model-facing string (spec-auditor finding, D5) · never.
- 2026-09-06 · D5 · Output records may declare `Optional<T>` components: optional and nullable in the derived schema, absent or null → `Optional.empty()`; every other component is required. Jackson's missing/null-creator checks are off because the validator enforces required-ness first · the router verdict's subject and node guesses are best guesses (TECH_PLAN §4.2), and a required-everything schema would have rejected them at D37 · never.
- 2026-09-06 · D5 · The breaker refuses a call at spend ≥ cap (the cap is a ceiling: once reached, the next call is refused); the retry count (2), backoff base (500 ms) and on-demand batch concurrency (4) are §4.11 design constants in code, not `margai.*` config · they are resilience policy, not product limits; TECH_PLAN §4.8 says "over budget" without an equality rule · if ops needs to tune them.
- 2026-09-06 · D5 · Embedding input tokens for the ledger come from the `X-Amzn-Bedrock-Input-Token-Count` response header, else the body count (Titan `inputTextTokenCount`), else a ≈ 4-characters-per-token estimate with a WARN — never silently zero · §4.8 "tokens from the response"; a zero would starve the breaker (spec-auditor finding, D5) · D17, when the live embed path runs.
- 2026-09-06 · D5 · Prompt files are StringTemplate 4 **group** files `prompts/<name>.v<N>.stg` with a `system` template (the cached prefix) and a `user` template; `PromptRegistry` loads them at startup, refuses duplicates, and takes the active version from `margai.ai.prompts.<name>.version`, else the highest present · one file per prompt version with the cache boundary explicit; group imports can share a persona later (TECH_PLAN §4.12 said `.st`) · never.
- 2026-09-06 · D5 · `FakeAiClient` ledger rows carry the configured tier model id and realistic usage (≈ 4 characters per token; the system prefix is a simulated cache write on the first call per prompt version and a cache read after) · cost and breaker behave as in production in every profile (DEV_SPEC §13.7 item 6); fake vs live is a profile fact, not a row fact · never.
- 2026-09-06 · D5 · Fake fixtures `ai-fixtures/<prompt>.<case>.json` hold the output object only; `variables.fixture_case` picks a case, otherwise a deterministic hash of the rendered user prompt picks among the public cases; `_`-prefixed cases are failure cases reachable only by name; `<case>.repaired.json` answers the repair retry · the same question always gets the same answer locally, and failure paths cannot leak into ordinary runs · D23, when the eval's fake layer sets cases per fixture id.
- 2026-09-06 · D5 · Structured output: victools jsonschema-generator 5 (Jackson 3 line) derives the tool schema from the output record — snake_case names, every component required, `additionalProperties: false`, enums by constant name — and networknt json-schema-validator 3 validates the model's tool input against that same schema before Jackson 3 decodes it (TECH_PLAN §4.11, D3.21) · one schema object serves Bedrock and validation · if either library fights a Jackson upgrade (the in-house record-schema fallback stays an option).
- 2026-09-06 · D5 · `completeBatch` is the bounded on-demand loop (concurrency 4, virtual threads) as an `AiClient` default method over `this.complete`, so the outermost decorator runs every record through the whole chain; Bedrock batch-job submission and the `batch_inference` flag land with the first ≥ `batch_min_records` caller (D55+) · no caller can reach the minimum before the nightly brain (TECH_PLAN §4.11) · D55.
- 2026-09-06 · D5 · Retries live in `RetryingAiClient` only (two jittered retries on throttling and 5xx); the SDK client runs with `AwsRetryStrategy.doNotRetry()` and `apiCallTimeout` = `margai.ai.call-timeout` (20 s); timeouts are not retried · one retry policy, not two multiplied; §4.11 names throttling and 5xx · never.
- 2026-09-06 · D5 · `RouteDecision` has three factories — `verification()`, `generation()`, `router(RouterVerdict)` — and an ArchUnit rule confines callers of `router(...)` to `ai.tasks.DifficultyRouter` (D37); §4.2's "compile-time property" is an architecture-test property, since Java cannot scope a constructor to one class in another package. A tier-rule breach is a `TierPolicyException` (an `IllegalArgumentException`: caller bug, not a model outcome) · the type keeps REASON honest without a second package · never.
- 2026-09-06 · D5 · `bedrock` profile activation: `MargaiApplication.main` adds the profile when `BEDROCK_LIVE=1`; tests use `@ActiveProfiles("bedrock")` behind `@EnabledIfEnvironmentVariable`; ECS sets `SPRING_PROFILES_ACTIVE`. The bedrock package exposes the live client as an `InnerAiClient` (not itself an `AiClient`) so the one `AiClient` bean stays unambiguous and only that package imports the SDK · TECH_PLAN §1.2 without config tricks; §1.4 import rule holds · never.
- 2026-09-06 · D5 · Cost: `paise = HALF_UP((in·p_in + out·p_out + cr·p_cr + cw·p_cw) / 1e6 × usd_inr × 100)`; batch rows use the batch input/output prices; `input_tokens` excludes cache tokens (Bedrock usage semantics — the smoke rows are the check); the ledger prices an `invalid_output` row for the tokens both attempts consumed, and a `breaker`, `error` or `timeout` row at zero · TECH_PLAN §4.8 at insert time · when Bedrock changes usage semantics.
- 2026-09-06 · D5 · The live smoke is a JUnit test gated by `BEDROCK_LIVE=1` (`BedrockSmokeTest`), two calls on the `cheap` tier, asserting a forced-tool result, real token counts and a prompt-cache read on the second call · TECH_PLAN §13.2 item 1 leaves both proofs to D5; assertions beat eyeballing; PLAN D5 says "one live call" — the second costs under a rupee · never.
- 2026-09-06 · D5 · ArchUnit through the `archunit` core artifact (1.4.2, the version Modulith already brings; no `archunit-junit5`); the "no model-id literal" rule is a source scan of `ai/**` (`ModelIdLiteralTest`) because bytecode offers no string-literal API; family prefixes that select a wire format (`amazon.titan`) are allowed · fewer dependencies, the rule still enforced · never.
- 2026-09-06 · D5 · Local defaults in `application.yml`: `usd_inr = 90`, `global_daily_paise = 50 000` (₹500 per IST day), `max-output-tokens = 1024`, `call-timeout = 20s`; beta values come from SSM (TECH_PLAN §7.3) · placeholders that make the breaker real in `local`; the founder took the recommended figures at the D5 approval · F8 SSM seeding.
- 2026-09-06 · D5 · The 20 `AiFeature` codes and the 5 `AiCallStatus` codes are the CHECK lists of TECH_PLAN §2.8 verbatim, lowercase enum constants (the D4 rule); `Tier` is `cheap|reason|vision|embed` · one spelling in DDL, Java and the ledger · with each new feature (one-line migration plus a constant).
- 2026-09-06 · D5 · `IstClock` lives in `common.api` with one bean (`common.internal.ClockConfiguration`); tests build one over `Clock.fixed` · TECH_PLAN §11.1 "the only way code learns today" · never.
- 2026-09-06 · D4 · `margai.ai.batch_min_records` default stays 100 and `completeBatch` submits with the same `global.` profile IDs as real-time calls (console check #2, TECH_PLAN §13.2 item 2) · the Bedrock quota "Minimum number of records per batch inference job" is 100 for Claude Haiku 4.5 and Sonnet 4.6, not adjustable, maximum 100,000; both models support batch only through cross-region inference profiles, whose region list includes ap-south-1 · if AWS changes the quota, or the beta grows past the minimum and the first real batch job (D55+) surfaces a limit.
- 2026-09-06 · D4 · EMBED = `cohere.embed-multilingual-v3`, on-demand in ap-south-1 itself (no `global.` profile), 1,024 dimensions; `amazon.titan-embed-text-v2:0` at 1,024 dimensions is the fallback by config (console check #4, TECH_PLAN §13.2 item 4) · the plan's first choice is available and proven with an English and a Hindi text; embedding traffic stays in Mumbai; the vector size matches `vector(1024)` without a migration · if retrieval quality on Hindi (D17 ✅) argues for Cohere Embed v4 via its global profile, which would also need an `output_dimension` of 1,024.
- 2026-09-06 · D4 · Ledger price table seeded from the Bedrock Mumbai global cross-region on-demand page (TECH_PLAN §13.2 item 1): Haiku 4.5 1.00 / 5.00, Sonnet 4.6 3.00 / 15.00 USD per 1M in / out, batch at half, cache read 0.10 / 0.30; cache writes billed at the 5-minute rate (1.25 / 3.75) because the plan's cached prefix is per request and 5 minutes is the default `cachePoint` TTL; `usd_inr` is config beside the table · §4.8 computes `cost_paise` at insert from config, never from code, and the table needs one write price per model · at every Bedrock price change, or if a 1-hour cache point is ever used (separate key).
- 2026-09-06 · D4 · Bedrock tier defaults from ap-south-1 (console check #1, TECH_PLAN §13.2 item 1): CHEAP = VISION = `global.anthropic.claude-haiku-4-5-20251001-v1:0`, REASON = `global.anthropic.claude-sonnet-4-6`; current Anthropic models are reachable only through `global.` inference profiles; Sonnet 5, Opus 5 and Opus 4.8 are gated for this account (AccessDenied, AWS Sales allowlist) and go on the F8 list as an upgrade request · founder's Converse probes: the listing shows what exists in the region, only a call proves what the account may invoke; Sonnet 4.6 is the strongest invocable model today · when the allowlist request lands, or the D23 eval shows REASON missing numericals.
- 2026-09-06 · D4 · ArchUnit arrives at D5 with the first import rule it enforces (only `ai` imports the Bedrock SDK); D4 ships Spring Modulith `verify()` alone · nothing to enforce yet · D5.
- 2026-09-06 · D4 · One Testcontainers container per test JVM, kept outside Spring's lifecycle, with one database per set of active profiles plus throwaway databases on demand (`TestcontainersConfiguration`) · Spring stops container beans when any context closes, including one that failed to start; the `test` profile's seed location would fail Flyway validation in a no-profile context sharing the database · never.
- 2026-09-06 · D4 · Seed rows carry fixed UUIDs (`00000000-0000-4000-8000-0000000000NN`) and upsert keyed on that id, so any value including a natural key can be edited and re-applied; edge rows (composite primary key) use `DO NOTHING`; the seed carries no rollback block (the table drops remove it); the three cutoff rows are synthetic and say so in `source` · a repeatable migration re-runs whenever its checksum changes, so it must be idempotent on a persistent local db, and an upsert keyed on `code` would break the moment a code changed (spec-auditor finding, D4) · never.
- 2026-09-06 · D4 · The `local` profile is switched on by `./mvnw spring-boot:run` through the Spring Boot Maven plugin configuration, not by `application.yml`; the packaged jar and tests set profiles explicitly · TECH_PLAN §1.2 "developer default" without a deployed environment ever inheriting the seed location · never.
- 2026-09-06 · D4 · Undo convention: every migration's header carries `-- ROLLBACK:` … `-- END ROLLBACK`, one statement per line (executed line by line); a `rollback/U<n>__*.sql` file takes precedence when present and is handed to the driver whole, so it may contain function bodies; `MigrationReversibilityTest` runs them newest first on a throwaway database and also rejects PostgreSQL enum types · TECH_PLAN §2.9 and §8.2 made executable · never.
- 2026-09-06 · D4 · Prerequisite edge semantics: `from_node_id` must be learned before `to_node_id` (from is a prerequisite of to) · replaces DEV_SPEC §3.2's `prerequisites UUID[]` with a directed join table; the D13 loader follows it · D13.
- 2026-09-06 · D4 · Value types that more than one module stores (`AttemptType` for `student_profiles.attempt_type` and `archetype_tracks.code`, `Category` for `student_profiles.category` and `cutoffs.category`) live in `common.api`; `common` therefore exists from D4 · account and curriculum may not depend on each other (TECH_PLAN §1.4) and two identical enums would drift · never.
- 2026-09-06 · D4 · Enum constants are the lowercase database and wire codes (`Subject.physics`, `AttemptType.fresher_1yr`) with `@Enumerated(EnumType.STRING)` · one spelling in DDL, Java and JSON (TECH_PLAN §2.1, §11.3); no converters · never.
- 2026-09-06 · D4 · Cross-module references are id columns with database foreign keys, never JPA associations (`chapter_status.user_id`, `student_profiles.user_id`); associations are allowed inside one module · a module owns its tables and other modules never read them directly (TECH_PLAN §1.3); the Modulith test would reject the import · never.
- 2026-09-04 · D4 · Database locked: PostgreSQL 18.6 on db.t4g.small, single-AZ, 20 GB gp3 + autoscaling, 7-day backups, deletion protection; pgvector 0.8.1 confirmed via AWS release notes; console create-screen initially hid 18.x — engine/class facts come from the API (`describe-db-engine-versions`, `describe-orderable-db-instance-options`), extension facts from release notes and `pg_available_extensions` · console check #3 (TECH_PLAN §13.2 item 3) closed 2026-09-04; the PG17 fallback (§0.5 item 7) was never applied and is void · minor-version bumps at maintenance windows.
- 2026-09-04 · D3 · Minors: the parent-consent OTP is part of the onboarding flow, and until consent is complete `CONSENT_REQUIRED` covers photo doubts as well as document uploads; text features stay available; after consent everything unlocks (TECH_PLAN D3.28, founder decision 8) · SPEC §6.8 says "before any upload"; a doubt photo is an upload · never.
- 2026-09-04 · D3 · The anonymous peer percentile is shown only when the cohort (same attempt type, active in 14 days) has ≥ 30 students (TECH_PLAN D3.27) · Evidence rule at beta scale · D58.
- 2026-09-04 · D3 · A doubt follow-up weighs 0.5 toward the free-tier limit, like a cached hit (TECH_PLAN D3.26) · SPEC §6.3 is silent; follow-ups reuse context and are usually CHEAP · D44 metrics.
- 2026-09-04 · D3 · Crash reporting through PostHog error tracking, within the three-SDK rule; founder-confirmed at approval, Crashlytics only via an explicit rule amendment (TECH_PLAN D3.25, decision 4) · SPEC §11 needs crash-free % without a fourth SDK · D73.
- 2026-09-04 · D3 · Java records, sealed types, constructor injection; no Lombok (TECH_PLAN D3.24) · Java 25 makes Lombok unnecessary · never.
- 2026-09-04 · D3 · REASON tier reachable only with a `RouteDecision` from three named producers: router, verification, generation (TECH_PLAN D3.23) · the "router only" rule as a type · never.
- 2026-09-04 · D3 · Doubt images deleted immediately after extraction, documents after confirm/discard; the `expires_at` sweeper and the 1-day lifecycle are backstops (TECH_PLAN D3.22) · the 24-hour promise becomes minutes · never.
- 2026-09-04 · D3 · Structured model output via Bedrock Converse forced tool use, validated against the record's JSON schema (TECH_PLAN D3.21) · no JSON parsing heuristics · never.
- 2026-09-04 · D3 · Seed data as a repeatable Flyway migration in a `db/seed` location enabled only in the `local` and `test` profiles (TECH_PLAN D3.20) · production never sees test data · never.
- 2026-09-04 · D3 · `AiClient` = `complete`, `completeBatch`, `embed`; feature tasks around it; ledger, breaker, tier policy, schema validation and retry as decorators (TECH_PLAN D3.18) · stable seam; every rule enforced once · never.
- 2026-09-04 · D3 · Doubt cache keyed by `(question_hash, language)`; a cross-language exact hit renders the verified canonical answer (CHEAP) and counts as a hit (TECH_PLAN D3.17) · keeps the 55% hit-rate target reachable in three languages · D41 metrics.
- 2026-09-04 · D3 · Join tables instead of `UUID[]` where foreign keys matter; plan blocks are rows (TECH_PLAN D3.16) · referential integrity; blocks are addressed by id · never.
- 2026-09-04 · D3 · The nightly run is the same image as a scheduled ECS task (`jobs` module); notifications are scheduled from `PlanGenerated` events and dispatched by the API's in-process scheduler from `notification_log` (TECH_PLAN D3.15) · API latency unaffected; one dispatch mechanism · second API task.
- 2026-09-04 · D3 · Spring Modulith verifies module boundaries in a test from D4 (TECH_PLAN D3.14) · rules a test enforces, not a paragraph · never.
- 2026-09-04 · D3 · Config via `@ConfigurationProperties` records bound from environment variables; `application.yml` holds non-secret local defaults (TECH_PLAN D3.13) · fail-fast validation, no runtime SSM client · never.
- 2026-09-04 · D3 · JWT HS256 15 min + opaque rotating refresh tokens (30 d) with family reuse detection (TECH_PLAN D3.12) · DEV_SPEC §5 made concrete · never.
- 2026-09-04 · D3 · IST for every "today"; `TIMESTAMPTZ` storage; paise integers; UUID v4 keys (TECH_PLAN D3.11) · correctness at the day boundary; boring ids · never (uuidv7 parked).
- 2026-09-04 · D3 · One AWS environment `beta`: Fargate in public subnets behind the ALB (no NAT gateway), RDS single-AZ, secrets injected by ECS from SSM (TECH_PLAN D3.10) · cost and simplicity · public launch.
- 2026-09-04 · D3 · Flutter: go_router, dio, flutter_secure_storage, Riverpod without codegen, drift at D34, flutter_markdown_plus + flutter_math_fork (TECH_PLAN D3.9) · boring, well-supported; codegen only when drift forces build_runner · D34; markdown package health at D32.
- 2026-09-04 · D3 · In-process rate limiting, dispatcher, sweepers and async listeners; single API task (TECH_PLAN D3.8) · beta topology; upgrade path in TECH_PLAN §13.3 · second API task.
- 2026-09-04 · D3 · pgvector HNSW cosine indexes, not ivfflat (TECH_PLAN D3.6) · no training step, better recall at this scale · > 5M vectors.
- 2026-09-04 · D3 · Enumerations as `VARCHAR` + `CHECK`, never PostgreSQL enum types (TECH_PLAN D3.5) · one-line migrations to extend · never.
- 2026-09-04 · D3 · Content pipeline in Java inside the server (`pipeline` profile, picocli), VISION-tier page extraction; `pipeline/inputs/` holds founder data, `pipeline/reports/` the run reports; founder-confirmed at approval (TECH_PLAN D3.4, decision 6) · pipeline.md requires `AiClient`; one toolchain; layout-proof extraction · D14 if extraction quality disappoints.
- 2026-09-04 · D3 · Hinglish is the `hi_Latn` locale (`app_hi_Latn.arb`); server value `hinglish` (TECH_PLAN D3.3) · valid BCP-47 tag, zero custom plumbing; verified with gen-l10n · never.
- 2026-09-04 · D3 · One technical plan document, `docs/TECH_PLAN.md`, cited by section (TECH_PLAN D3.1) · one place for rules and agents to point at · never.
- 2026-09-03 · D2 · One JDK for everything: Flutter's Gradle (9.3.1 / AGP 9.1) builds on the same JDK 25 the server uses, so no `flutter config --jdk-dir` · fewer moving parts in the fresh-clone path · if a Flutter upgrade pins an older Gradle.
- 2026-09-03 · D2 · Actuator exposes only `/actuator/health` with component names, no details · boot proof needs the db component; details wait for auth (D7+) · D7.
- 2026-09-03 · D2 · Android SDK via Homebrew `android-commandlinetools` (no Android Studio); emulator optional through `scripts/dev-setup.sh --emulator` · lighter, scriptable, fits the 15-minute fresh clone · when a contributor needs the IDE.
- 2026-09-03 · D2 · Generated localisations `app/lib/l10n/app_localizations*.dart` are committed · Flutter dropped synthetic l10n packages; CI and IDEs need no extra step · never.
- 2026-09-03 · D2 · Flutter dependencies at D2 are `flutter_riverpod` and `flutter_localizations`/`intl` only; drift arrives with the offline work (D34) · YAGNI · D34.
- 2026-09-03 · D2 · Flyway is wired at D2 with an empty `db/migration` location that carries a README so Flyway's location check passes; the first migration lands at D4 · PLAN D4 owns the first migrations (see the D2 day log for the DEV_SPEC §13.8 wording and how it was resolved) · D4.
- 2026-09-03 · D2 · Server tests use Testcontainers with `pgvector/pgvector:pg18`; the running app uses the compose db · tests never depend on local db state; same image as compose · never.
- 2026-09-03 · D2 · Spring Boot pinned to 4.1.0 on Java 25 from the Homebrew `openjdk@25` formula (no sudo), linked into `~/Library/Java/JavaVirtualMachines` · latest GA of both; CI already pins Java 25 · each Spring Boot minor.
- 2026-09-03 · D2 · Java package `com.margai`; Android applicationId and namespace `com.margai.app` · the specs name no package; three segments is the Android convention · before the first Play upload (D74) or when the final name lands (TRACKER F6/F7) — the applicationId is permanent after upload.
