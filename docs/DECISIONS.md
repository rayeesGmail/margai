# Decisions log

Where docs/SPEC.md and docs/DEV_SPEC.md are silent, CLAUDE.md says: choose the boring, maintainable
option and record it here. One line per decision, newest first. Conflicts between documents are not
recorded here — they are surfaced in the session and resolved in the documents themselves.

Format: `date · day · decision · why · revisit when`

- 2026-09-16 · D15 · **The two flags of the chapter-7 re-read are `misprint` entries: the book prints "proportional to be cube" (p7) and "the acceleration due gravity decreases" (p8), and the transcription kept both** (founder, "approved as written, recommended option per ruling") · each was read against the page rendered at 3× — page 8's text layer is private-use encoded, so the image is the only witness — and the verifier had quoted the grammatical form in each case. That is now its fourth and fifth silent repair of the book's own writing (with `r_E` → `R_E` and the invented "total energy of a satellite"), and on the "to be cube" span it did the same in the seeded run, so it is habit: **the verifier reads a page as it should have been written**, which is a false-flag class to expect in every book and never a reason to change a transcription · if a book's adjudication ever shows the opposite — the verifier accepting a misprint it should have flagged.
- 2026-09-16 · D15 · **A read of a page is matched to a row by the hash of the part it read, not by the address the row had when the page was read** (founder, "approved as written, recommended option per ruling", ruling 2) · a join or split moves every later row of its section down a number, and chapter 7's four splits left 30 rows without a verdict where only 8 of them — the four shortened halves and the four new ones — carry words the artefact has not been read against; paying to re-read a page whose words are identical buys nothing and costs twice — the money, and a fresh draw on a page already adjudicated, which can raise new wrong signals to rule on. A read made of exactly the page's parts is mapped to them in order, which is right whatever the rows are called now and right where a page prints the same words twice; where the page's parts have changed, a part whose words appear once in the read keeps its verdict and an ambiguous one is left without any — the address recorded in the artefact is the address before the renumbering, so it cannot be trusted to break the tie (corrected after the spec-auditor's MINOR, 2026-09-16) · if a book's pages repeat whole paragraphs often enough that pages are being re-read for ambiguity alone.
- 2026-09-16 · D15 · **A printed start whose math the text layer dropped is paired with its row: after the ordinary comparison, what is left over on both sides is matched once more where the layer's letters all appear, in order and close together, at the row's opening** (founder, ruling 3) · chapter 7 page 7 counted 10 rows against 10 printed paragraphs and still flagged, because the layer maps no glyph for the symbol fonts and gave "For , using binomial expression," for the row's "For h/R_E << 1, using binomial expression," — a false flag of a class that recurs wherever a paragraph opens with math, across ten books. Only the layer may be missing letters: a row missing the print's is a defect this must not hide. What keeps it honest is that only leftovers are paired — everything the ordinary comparison matched is already gone — that the letters must agree from the first and then run in order within a short reach, and that every pairing is named in its own report section, so a page it quiets still says so (the section added after the spec-auditor's MAJOR, which also struck out a claim that the page's counts agreeing was the safeguard: a pairing removes one from each side, so a mismatch survives it and agreement guards nothing) · if a real defect is ever found hidden by a pairing — the named list is where it would be found.
- 2026-09-16 · D15 · **A free check holds each page's printed equation numbers to the numbers its rows carry, counted, and speaks only where the print carries one more often than the rows** (founder, ruling 4) · the paid second read missed a dropped displayed equation on both draws (the row above), and the number beside such an equation is the part of it the text layer keeps; counting rather than listing is what makes it work, since a page prints the same number both beside its equation and in the sentence referring to it. Previewed at ₹0 over the real rows: silent on chapter 7 as corrected, and on the seeded copy it names both the dropped (7.35) and the altered (7.12). It closes the blind spot only for a **numbered** display — an unnumbered one is still nobody's — and its flag names the several things it can mean (a display dropped, its number altered, a reference to it lost), not one · if a book's transcription writes equation numbers in a form the check cannot see (silence on a book that has them), or if a page-break split between a number and its rows makes it speak where nothing is wrong.
- 2026-09-16 · D15 · **A chapter already adjudicated keeps its extraction through its book's corpus event: the book is extracted without `--redo`, which resumes over that chapter's pages, and its JSONL is copied aside rather than moved** (founder, ruling 5) · `phy11-part1` chapter 7's corrections are written against Opus run 11's exact words — each a span that must occur once on its page — so re-extracting it would strand its five text-changing entries by name (and leave its two rulings on flags judging spans nobody quoted any more), costing the whole adjudication again; the chapter was extracted on the same frozen prompt and model as the rest of the book will be, so keeping it costs nothing in consistency · if a prompt version ever changes mid-book, when the whole book is re-extracted together.
- 2026-09-16 · D15 · **Three more entries in `ncert-corrections.yaml`: `Fig. 7.5` removed from Example 7.2's stem, and the second read's two remaining signals ruled `false_positive`** (founder, ruling 1) · all three were read against the rendered page on 2026-09-15 and scored — the figure ref belongs to part (b), the page prints `r_E` where the verifier "corrected" it to `R_E`, and the page prints "an circularly orbiting" where the verifier invented "a satellite"; a ruling is how a scored flag stops being raised again, and a `false_positive` needs no reload because `ncert verify` reads the file itself · never — a ruling is a record; it is revisited only if the page is re-read and found to say something else.
- 2026-09-15 · D15 · **On the seeded recall run (9 of 12 found; missed the dropped prime, ≅ flattened to =, and a dropped displayed equation; 6 wrong signals): the three missed pages are read again before any decision; code first sets aside omitted entries that are a section heading or a figure/table caption, and a trailing `?` or `!`; and the paid second read is kept for the book runs — with its blind spots named in the runbook and the founder's spot reads aimed at primes, approximation signs and displayed equations if the misses repeat, without that caveat if they do not** (founder, "approved as written, recommended option per ruling") · one run of twelve pages on a model sampling at its default temperature cannot tell a habit from a draw (the freeze row's repeat-before-change), and ₹5 settles it; four of the six wrong signals were headings and captions code can recognise; nine real defect classes found per twelve are worth ≈ ₹1.5 a page against one wrong anchor reaching a student (DECISIONS 2026-09-14, quality over call cost) · after the repeat; and again after the first whole book's adjudication, on its real flag-to-defect ratio. · **the repeat, 23:04: the three misses are habit** — each missed on both draws (the verifier read the printed ≅ as =), so the second read is kept with those three blind spots named in the runbook.

- 2026-09-15 · D15 · **The chapter-7 pass mark is void, and the verifier's recall is measured on seeded defects before any book: a scratch copy of chapter 7's rows carries about a dozen injected defects of the classes that matter, read with `ncert verify --read-pages` under its own artefact tag; beforehand code sets aside three false-flag classes the calibration showed — a radical written `√x` or `sqrt(x)`, a single-token exponent written `^-n` or `^(-n)`, and a trailing full stop** (founder, "approved as written, recommended option per ruling", rulings 1 and 2) · the must-find was the book's own print (page 4 prints r̂ in all three forms of Eq. (7.5)), so a correct verifier could never meet the bar; the calibration's 8 second-read signals were all wrong and it found nothing real, while the free checks found 5 real defects — which says run 11 has no character defect left for recall to be measured on. A seeded run has a ground truth the page cannot supply · after the seeded run: if recall is poor, the paid second read is dropped for the free checks and the founder's spot reads; if it is good, the false-flag rate decides whether the book runs carry it.
- 2026-09-15 · D15 · **`ncert-corrections.yaml` gains two kinds: `figure_ref` (a span once on the page names the paragraph; exactly one of `remove` or `add` names the label) and `drop` (the paragraph starting with `at` is removed from the page)** (founder, ruling 3) · the calibration's real findings include a figure ref filed on the wrong paragraph (Example 7.2's stem carries `Fig. 7.5`, which only part (b) mentions) and a possible caption transcribed as a paragraph; neither could be said in the file, so the only remedy was a re-extraction that re-draws the page · when an adjudication needs to move a paragraph to another section (PARKED).

- 2026-09-14 · D15 · **`ncert verify` counts only the ruling's verifier, only live, and never a misquote: `--read-pages` refuses before any call on the fake client and on a verify tier that is not `margai.pipeline.verify-model` (`claude-sonnet-5`); a read or a stored verdict by any other model or prompt version is neither reused nor counted; a claimed difference whose transcribed span the row does not carry leaves the row `not_judged`; and every run, the free one too, re-judges the rows from the artefact** (spec-auditor FAIL on the first build, MAJOR ×3, fixed before any live run) · the independence guard alone read only the transcriber's model, so a forgotten `visionsonnet` profile would have verified with the default VISION model and a forgotten `AI_LIVE=1` would have recorded fixtures on the real rows — and a later correct run would have skipped those pages as already read; a misquote had been counted a match and so clean, a verdict the second read never gave; and rulings had applied only inside a paid run, against the runbook's own promise · when the pair changes (a new model or a new ruling): the pin is config, the refusal is the code.
- 2026-09-14 · D15 · **`ncert verify` calls one page at a time through `complete`, not `completeBatch`, which `.claude/rules/pipeline.md` otherwise asks of pipeline AI calls — the same exception `ncert extract` has, for a different reason** (spec-auditor MINOR) · the pages are independent, so batching is possible; but `completeBatch` is today the bounded on-demand loop that runs to completion and rethrows its first failure, which would lose the answers of pages already paid for, while one call per page lets every page reach the artefact before the next is sent and a stopped book resume at no cost. The estimates (₹1.05 a page) are at on-demand prices · D55, when the real batch lane lands with per-request results: a whole-book verify is the natural first batch caller at half the price.
- 2026-09-14 · D15 · **The clean share (PLAN D15 ✅, "% paragraphs extracted cleanly per book") counts a paragraph clean when its second read matches — after code's set-asides and the founder's rulings — and no join or figure flag names it; the two signals that name no row, page-level start flags and running text no row carries, are counted beside the share and must be adjudicated before it is recorded; the book line appears only when every chapter is selected, has loaded rows and every row has a verdict** (spec-auditor MINOR: the definition was the build's, not the plan's) · PLAN D15 says only "extracted cleanly" and the D14 standard was text and address right; a start flag or an omitted passage is real evidence about segmentation and lost text but belongs to a page, not a paragraph, so folding it into a per-paragraph share would be a guess about which row it spoils · at the first book's adjudication, if page-level signals turn out to map to rows often enough to count them.
- 2026-09-14 · D15 · **`NcertPage`'s decode refusals keep their wording, which tells the model what a paragraph must carry, although `.claude/rules/ai-layer.md` keeps model-facing strings out of Java** (spec-auditor re-audit MINOR; `PageVerdicts`, new this build, was stripped to fault statements) · the message reaches the model as the repair turn of the frozen `ncert_extract` v3's calls, so rewording it changes the transcriber's behaviour under a frozen prompt, and every dry-run measurement of 2026-09-14 was taken with it · at v3's next amendment, when the repair wording moves into the `_protocol` fragment group with the prompt change and its changelog row.
- 2026-09-14 · D15 · **`ncert_verify` v1's examples are invented and none is chapter 7's text or defect; a test keeps the calibration's must-find out of the prompt; the only chapter-7 material in it is the notation block copied word for word from the frozen `ncert_extract` v3** (the prompt does teach the must-find's class by name — a hat written by analogy — so a find on the vector r is a taught class found in unseen text, and the runbook says so) (spec-auditor BLOCKER on the first build: the worked examples were chapter 7's own defects with their expected spans, which would have made the calibration a pass by construction) · a calibration measures a reader on a chapter it has not been told about; the copied block's chapter-7 symbols (`F'_GB`, `g(h) ≅`, `F_GA`) are conventions both the transcriber and the verifier were given, so a find on exactly those counts for less and the runbook says so · never for chapter 7; when a second calibration chapter is chosen, the same test holds its defects out.

- 2026-09-14 · D15 · ~~**The verifier's chapter-7 calibration passes when the vector r in §7.3 ¶5 is flagged and no more than 10 of its flags on the 102 rows are wrong against the page**~~ — **void 2026-09-15** (row above): the must-find was the book's own print, not a defect. Kept for the record: (founder, plan question 5, recommended option; the plan approved with "ok, let's implement it") · a bar set before the run is the only honest one; the vector r is the one defect the eleven dry runs proved Opus repeats (four draws of four) and Sonnet can see (one read of four), so it is the calibration's must-find, and ten wrong flags in 102 rows is the most a person adjudicating a 1,000-row book (~100 wrong flags) can reasonably absorb · if the vector r is missed, the call unit moves from one page to one paragraph per call (about 3.5× the cost) as a build change; if false flags exceed ten, the set-asides and the prompt's "not a difference" list are read against the wrong ones before any book.
- 2026-09-14 · D15 · **`ncert verify [--read-pages]` reads one page per call — the transcriber's bands, never the text layer, every paragraph's part on that page as a numbered item, one fixed question per item (`matches | differs | not_on_page`) plus the running text no item carries — refuses to run when the ledger says the verify tier's model transcribed the rows, keeps the model's answers as given in `verify/{book}/{lang}.jsonl`, and records a verdict on each row whose pages were all read** (founder, plan questions 1 and 2, recommended options) · one call per page is the unit the ruling was costed on — about ₹1.05 a page, ₹14 for chapter 7, ₹190 for phy11-part1, ₹2,000 for the ten English books, against ~₹0.75 a paragraph (₹80 / ₹770 / ₹8,000) — and it is the only unit that can ask what the page prints that no paragraph carries, the one defect of the dry runs (three equations lost, run 9) no per-paragraph judgement or text-layer ratio could see. The text layer stays out because the transcriber was told to trust it: a verifier handed it would share the misreadings it exists to catch. The guard reads the ledger rather than a flag because a forgotten profile would otherwise verify Opus with Opus in silence. The raw answers are stored and code's judgements applied at report time, so a ruling added later costs nothing already read; a verdict names the SHA-256 of the text it read, so a load that keeps the text keeps it and a correction drops it · after the calibration; and at D16, where the Hindi pages' illegible layer withholds the free checks and the second read is the only machine check Hindi has.
- 2026-09-14 · D15 · **Code, not the model, sets aside spacing and the ruling's glyph variants (≅ ≃ ≈ with "approx=", the minus and its dashes, quotation marks, × and ·), and a transcribed span the row does not carry; brackets are left to the prompt, which flags a bracket only where it changes what an expression means** (founder, plan question 3, recommended option) · HOW TO RESUME's normalisation list said "ignoring bracket placement", while PARKED's fraction-bracket item wants the second read to catch `G Mm / d^2 L` for G (Mm/d²) L — which reads as d²L in the denominator — so code cannot drop brackets without hiding exactly that; what code can compute it decides (.claude/rules/ai-layer.md), and whether a bracket changes a grouping it cannot · when the calibration shows bracket flags that are all noise, or a grouping error the prompt missed. · **narrowed the same night (spec-auditor re-audit, MAJOR) — confirmed by the founder 2026-09-15 ("confirm the bracket narrowing")**: a rule that a factor after a fraction must be bracketed contradicted the frozen `ncert_extract` v3 conventions, which write `G M m / r^2` and `Gm(2m) / 1 j_hat`, so the verifier would have flagged convention-following rows across the corpus. The verifier now flags a bracket only where it changes what a sum or difference, an exponent or a function covers — groupings v3's own rules bracket — and leaves the grouping of products and quotients unflagged. **Consequence: the PARKED `G Mm / d^2 L` case is not caught by the second read.** By the freeze row (a defect the second read cannot catch is the only reason to amend v3), it becomes v3's first real amendment candidate: a rule that a factor following a fraction is bracketed apart from it, with the verifier's rule widened to match in the same change.
- 2026-09-14 · D15 · **The outcome of adjudicating a verify report is `pipeline/inputs/ncert-corrections.yaml`, keyed on the page and a span that must occur on it exactly once — never on a paragraph address — with five kinds: `text`, `join` and `split` change the frozen run's pages in `ncert load` after its page-break repairs and before numbering; `misprint` and `false_positive` change nothing and suppress the same flag in later verify reports** (founder, plan question 4, recommended option: segmentation kinds now, not later) · the load assigns addresses and a join or split moves every number after it, so an address-keyed entry would silently land on a different paragraph once another correction is applied, while the page and its words do not move; a span found zero times means the page is not what the entry was written against, twice means the entry does not say which, and both refuse by name. Segmentation kinds are needed on the very first adjudication — chapter 7's §7.6 ¶8 "and hence…" should join ¶7 — and the alternative, `--redo`, re-draws a frozen run's page and moves its boundaries by chance (dry runs 10 and 11). HOW TO RESUME named the fields (address, printed span, transcribed span, reason); the address stays in an entry for the reader and is never used to apply it · when an entry needs to move a paragraph's section, which no kind can do yet — re-extract that page until then.
- 2026-09-14 · D15 · **The free half of verify reads where the print starts paragraphs from the PDF's glyph positions (`PdfLayout`), with rules measured on keph107.pdf — a first line 12–40 pt in from the line below it (18 pt on this corpus), the first line after a bold section heading, an Example/Answer/Solution label, a numbered law or item marker, columns told apart by the leftmost margin three prose lines share, a left column whose first layer line sits more than three body lines below the right column's undecided — and its flags route attention and never refuse** · the dry runs showed the character layer stable and the segmentation not on a frozen prompt, and the book sets a boundary in a way code can read; on chapter 7's 102 Opus rows, before any tuning to them, printed starts equalled the rows on 6 of 12 pages, and after two misreads found against the rendered pages were fixed (a verso right column left of the middle; equations with no text layer at a column top), the preview raised 7 page-level start flags, 1 figure flag and no join flag, at least two of them real defects of run 11. A chapter whose layer is illegible (every Hindi book, kech202) gets no free checks, by the same per-chapter judgement extract makes · after the calibration measures their false-positive rate, and after the first Biology chapter, whose one-column pages and figure density the rules have not met.

- 2026-09-14 · D15 · **The pair: Claude Opus 5 transcribes, Claude Sonnet 5 verifies** (founder, ~21:25, "opus transcribes and sonnet verifies") · from eleven v3 dry runs on chapter 7, every row read against the rendered pages (TRACKER day log, the table): Opus structurally clean on both full runs and on three draws of pages 4, 6 and 8 with zero character changes between draws, text 98 of 99 on the amended prompt, closer to the print than either other model (the book's misprints kept); Sonnet structurally wrong in two full runs of three (run 2 the heading, run 9 page 5's column order — three equations lost, four §7.4 paragraphs filed under §7.3) but the one read of any model to see the vector r that Opus wrote `r_hat` four times of four; Haiku out after three runs with three different layout failures on the two-column page. The verifier's value is independence and a verdict per row, so Sonnet's layout failures do not disqualify it when it is handed one paragraph and one band. Cost about ₹8,500 Opus plus ₹2,000 Sonnet for the ten English books, against about ₹3,300 plus ₹5,000 for the reverse pair. Supersedes RULING 1 (2026-09-13, the reasoning model closed for extraction) and closes the Opus measurement row of this date · consequences: `ncert verify --read-pages` is the next build — Sonnet reads each loaded paragraph against its band with a fixed question, the code checks count printed paragraphs per page against rows and check every cross-page join, verdicts go on the row and into the report, the founder adjudicates in a session and the outcome is a corrections file under `pipeline/inputs/` that the load applies deterministically; chapter 7 is its calibration set (it must flag the vector r and little else); the transcription tier keeps the `visionopus` shape and the verify step takes the `visionsonnet` shape, both settled as tier config at the build; the phy11-part1 corpus event runs on Opus only after the verifier exists; the prompt stays frozen. · **annotated the same evening (spec-auditor, D15)**: the ruling's premise for keeping Sonnet — that it is "handed one paragraph and one band", with no layout to get wrong — was changed by the build's plan question 1 to one page's bands and its paragraphs as a list, plus a question about text no paragraph carries, which does ask page layout of Sonnet, the skill it failed in run 9; the per-item judgement still needs no layout. The chapter-7 calibration measures both before any book, and one paragraph per call stays the fallback (row above). · **corrected 2026-09-15**: the ruling's one argument for Sonnet as verifier — "the one read of any model to see the vector r" — was a misreading. Page 4 prints r̂ in all three forms of Eq. (7.5); Opus kept the print four times of four and Sonnet run 9 corrected the book. The evidence for Opus as transcriber is stronger for it; the case for Sonnet as verifier now rests on independence alone, and the seeded recall run decides whether the paid second read earns its place.
- 2026-09-14 · D15 · **Prompt `ncert_extract` v3 is frozen as it stands (commit debb920, after the five notation rules): no rule is added per run from here, and the next measurements repeat the same prompt on the same pages** (founder, ~18:50, "freeze the prompt", after asking why every run created a new issue) · nine runs carried nine prompts, and Sonnet 5 and Opus 5 refuse a temperature so every call is a fresh draw at the API default — so no run could say whether a new defect was the new rule or the sample; the D14 glyph table has been clean on runs 7, 8 and 9, and what remains is segmentation, which the page-image second read and code checks exist to catch · first repeat: Opus on chapter 7 pages 4, 6 and 8 with `--redo` (~₹20), read against run 8's rows; then `ncert verify --read-pages` is built against a prompt that does not move under it. An amendment after this needs a defect the second read cannot catch, and its changelog row.
- 2026-09-14 · D15 · **Claude Opus 5 is measured once on chapter 7 for the VISION tier (`visionopus` profile, adaptive thinking at low effort), and the decision it feeds is the transcriber–verifier pair, not a single model** (founder, ~16:45, "the pipeline is a one-time cost") · at $5 in / $25 out per million tokens Opus is 2.5× Sonnet and 5× Haiku, about ₹750 for phy11-part1 and ₹8,500 for the ten English books, small against one wrong anchor reaching a student; Sonnet already reads both dry-run chapters clean, so the question is whether the strongest model buys fewer judgement errors, and the guarantee still comes from a second independent read of every page · after the run: if Opus reads chapter 7 cleaner than Sonnet, Opus transcribes and Sonnet verifies; if alike, Sonnet transcribes and Opus verifies, the verifier's output being short.
- 2026-09-14 · D15 · **The page call carries one fact about the previous page and no words — whether its last paragraph ended without terminal punctuation — and the prompt states that a continuation can only sit at the top of the left or only column** (founder, ~15:50, on the third run) · with no tail and only a typography cue, Haiku read chapter 7 page 5 right column first, flagged the right column's mid-sentence top as the Answer's continuation, and never transcribed the left column's equations, "Alternatively…", part (b) or its primes — a loss the coverage ratio could not see because symbol-font mathematics barely registers in the layer; v2's three runs and v3's first, all with the tail, read the page correctly, so the tail's useful content was the fact that a sentence was open, which travels now without words to echo · if the fourth run reads page 5 wrongly, the flag itself moves to code or the page-image second read takes the decision.
- 2026-09-14 · D15 · **No text of the previous page travels with a page call — only its section — and the continuation flag is judged from the page's own typography; the prompt counts its headings before finishing; a numbered law or rule set as its own paragraph stays one** (founder, ~15:40, reversing the 200-character tail decided at ~10:05 on the first v3 measurement) · chapter 7 on both models: Haiku echoed the quoted tail on 2 of 12 pages — one verbatim, repaired; one paraphrased, "M_s is proportional to the cube of its radius", caught only by the character diff — while Sonnet echoed nothing but skipped the "7.3" heading and its paragraph at a column top and filed 14 paragraphs of §7.3 under §7.2, an address no loader check sees, and joined "3. Law of periods" onto "2. Law of areas" · after the second measurement — both models, chapter 7 and bio11 chapter 1 — to which RULING 1's reopening extends; the model is chosen on two runs each, on the same defect list.
- 2026-09-14 · D15 · **Paragraph numbers are assigned by `ncert load`, not by the model: prompt v3 returns section, text and one continues-previous-page flag per page, the loader counts ¶n per section in reading order across pages, and the quoted tail shrinks to 200 characters of context for that one flag** (founder, on Claude's recommendation, ~10:05; the reorder that put v3 before any whole-book bio11 run) · every defect that cost money on 2026-09-14 was the model reconstructing count and continuation from a quoted tail — a phantom empty §4.1 ¶5, an invented lead-in and repeated tail at §6.2 ¶47 on two calls out of two, ~16 unflagged continuations; a blank text or a misplaced flag is now refused where the output is decoded and re-called on the spot, an address collision cannot occur, and the label and repeated-tail repairs move a flag or cut a span instead of renumbering. Three prompt rules from the audits go in with it: a printed item marker is part of its paragraph, an equation number keeps its printed parentheses, a unit-opener biography page returns no paragraphs · if chapter 7 and bio11 chapter 1 on v3 do not read at least as clean as v2's, on the same defect list.
- 2026-09-14 · D15 · **`ncert load` deletes the rows of the chapters it carries that the extraction no longer produces, and names them; it refuses the whole load if any such row is anchored, and it spares a row that still holds the other edition's text** (supersedes the by-hand delete below; the `--prune` PARKED item closes) · with code-assigned numbering a one-page redo shifts every address after it, so orphans become the rule rather than the exception; the Hindi rule fell out of the existing test — a Hindi load cuts fewer paragraphs than the English one and must not delete English rows · D17, when the embedding joins the anchor in the guard and the migrate path makes the refusal a migration.
- 2026-09-14 · D15 · **A same-day re-run of a pipeline command writes `<date>-<command>-2.md`, `-3.md`, and never overwrites an earlier run's report** · the report is the day's committed evidence, and a one-page redo overwrote the whole-book extract report that the corpus-event commit was meant to carry · never.
- 2026-09-14 · D15 · **RULING 1 (the reasoning model is closed for extraction) is reopened for one measurement**: v3's two dry runs — phy11-part1 chapter 7 and bio11 chapter 1 — run on both Haiku 4.5 and Sonnet 5 and are read against the same defect list (founder, ~11:00) · RULING 1's evidence was two v1 runs whose failures were numbering (now code's) and section labelling (still refused by the loader); what remains with the model — reading order, box and biography detection, the continue-or-start judgement, rule-following — is where Haiku drifted on 2026-09-14 and where a stronger reader plausibly does better, at 2.2× the cost; plausibly is not measured · the eight books go on whichever reads cleaner; if equal, Haiku keeps the job.
- 2026-09-14 · D15 · **A corpus event deletes the book's paragraph rows by hand between the whole-book extraction and its load, so the table equals the run** (founder, run at 09:09 on the morning's finding; superseded the same day by the load's own prune, above) · `ncert load` reports the addresses a chapter no longer carries and never deletes them — right for a subset load into a full book, wrong for a re-extraction that replaces it: 85 rows from ten earlier runs sat beside the canonical 1,017, sampleable by the ✅ and embeddable by D17 · when `ncert load --prune` lands (PARKED), or at D17 when the freeze-and-migrate guard makes the deletion a migration.
- 2026-09-13 · D14 · **The VISION tier stays on the cheap model with tiling, and the reasoning-model track for extraction is closed** (founder RULING 1; `application-visionsonnet.yml` deleted) · measured on the same chapter, same prompt, same tiling: ₹27.96 against ₹12.71, **2.2× the cost**, and **neither of the two reasoning-model runs produced a loadable chapter**. Run 1 restarted paragraph numbering inside a tile band and collided two paragraphs at one address — an instruction the prompt gives explicitly and the cheap model obeys. Run 2, sent whole pages instead, named `Example 7.1` as a section, which is not a printed section number. Both runs deviated from the literal contract, and **the model's own confidence warned about neither** — it reported high confidence on the pages that failed the loader. The cheaper model is not the compromise here; it is the one that followed the instructions · if a defect class survives the v2 re-run that is about what the page *means* rather than about pixels or instructions — that is the only argument the closed track had.
- 2026-09-13 · D14 · **The page's own text layer is sent with the image and is authoritative for characters; the image is authoritative for layout, reading order, box detection, and is the fallback where a span of the layer is garbage** (founder FIX 1) · TECH_PLAN §6.1 chose images "because NCERT layout defeats text extractors". That reasoning held for *structure* and was wrong for *characters*: every residual defect of the D14 audit — `Gm(2m)/1` read as `/l`, `F'_GB` read as `F_GB`, a lost leading minus, `(1.52)^(3/2)` read as `^(1/3)`, dropped `×`, drifting subscript case — is a character these digitally-typeset files already carry correctly. Prose scored 98.96% with zero invention; the errors were concentrated in equations, where the glyphs are smallest and the text layer is *most* reliable, because the math fonts that garble extraction garble it visibly rather than subtly. Trust is judged **per chapter, not per page** — a page dense with equations scores low on English words by construction, and a per-page gate would withhold the layer from exactly the pages it exists to fix. Accepted cost: ~30–40% more input tokens per page (the founder's ruling; the re-run re-measures it) · never for English. For any book whose layer scores below `PdfTextLayer.MIN_LEGIBILITY` the image is already the only source, which is the mechanism, not an exception.
- 2026-09-13 · D14 · **Hindi extraction stays image-only, and the Hindi acceptance sample doubles to 30–40 paragraphs weighted toward matra- and conjunct-dense pages** (founder ruling on FIX 1) · all ten Hindi books are Chanakya glyph text with no ToUnicode map, so their text layer is not merely unreliable but meaningless — `PdfTextLayer.isLegible` scores them near zero and withholds it automatically. The consequence is that **Hindi loses the Fix-4 character check entirely**: English paragraphs are verified mechanically against the page, Hindi only by a native reader, so the human sample must carry the load the machine carries for English, and matra and conjunct density is where a vision model's Devanagari fails. This **raises the priority of the parked Chanakya→Unicode converter**: its output is not only an ingest convenience but the thing that would give Hindi the same mechanical verification English gets · **D16**, the first Hindi extract — if the converter has landed by then, Hindi joins the diff and the sample can return to twenty.
- 2026-09-13 · D14 · **One verified run per book is canonical and frozen; re-extraction is a versioned corpus event, not a re-run** (founder FIX 5 POLICY; supersedes the row below) · segmentation is not reproducible and chasing reproducibility was the wrong target — five runs of one chapter gave 135 / 149 / 124 / 120 / 110 paragraphs, and each differed for a *good* reason (a prompt fix, tiling, apparatus exclusion). What the corpus needs is not that two runs agree but that **one run is the corpus**: extract, verify, freeze, and treat any later extraction as a migration that re-embeds and re-anchors what it moved. The prompt version is what a freeze names, which is why the template becomes v2 (row below). **D17's guard therefore checks canonical-corpus integrity — is every paragraph of this book from the frozen run — and not cross-run stability**, which is neither achievable nor meaningful · when a second edition is ingested (the parked edition-versioning item), which is the first legitimate corpus event.
- 2026-09-13 · D14 · ~~**A chapter's paragraph set is free to change until something depends on it, and a migration afterwards**~~ — **superseded the same day by the founder's freeze policy above** (this row was written in the morning, before the work order): the set is frozen at the first *verified* run, not at the first *dependent* one, so a chapter cannot drift between its extraction and D17. What stands is the reasoning below, the section-level anchor argument, and the guard's home at D17 — now checking canonical-corpus integrity rather than cross-run stability. Kept for its measurements. Original: `ncert extract` may be re-run and `ncert load` may re-cut a chapter as often as the extraction improves, but once that chapter has embeddings (D17) or question anchors (D23) a re-extraction is a migration, not a re-run, and `ncert load` must refuse it without an explicit flag · paragraph segmentation is not reproducible and never was — three runs of the same chapter produced 135, 149 and 124 paragraphs, and the first two differed only in prompt wording, with tiling changing it again later. Sections are stable (7.1–7.10 in every run, each contiguous) and the student-facing anchor is section-level by SPEC §6.3 — "Class 11 Bio, Ch 6, §6.4" — so **no student ever sees a paragraph number and re-cutting does not move an anchor they can read**. What does break is what we build on top: a D23 question anchored to a paragraph id would, after a silent re-extraction, point at different text under the same heading, which is the one version of this that reaches a student · **D17**, where the first dependency appears and the guard therefore lands; the founder raised the risk at the D14 close on 2026-09-13, before any dependency existed.
- 2026-09-13 · D14 · **`has_equations` is computed in Java from the transcription and has left the model's tool schema** (founder FIX 3); the general rule is now in `.claude/rules/ai-layer.md`: **never ask a model to judge what code can compute** · audit item 9 — the model flagged `has_equations = true` on "3. Law of periods : The square of the time period…", pure prose with no expression in it. The field is a regex over text we already have: it costs nothing, it is deterministic, it can be corrected without a re-extraction, and it cannot hallucinate. Leaving it in the schema also spent output tokens on it per paragraph and gave the model one more thing to be confident about wrongly. It matters more than it looks, because a later equation-verification pass filters on exactly this flag · never.
- 2026-09-13 · D14 · **Every transcribed paragraph is checked against its page's own text layer by two threshold-free questions — is this word on the page at all, and is this symbol on the page — and a second, independent check compares the page's paragraph count against the shape of its text** (founder FIX 4, FIX 6) · every defect in the D14 table cost a human reading a rendered page against a database row to find; each now costs a glance at a report line. **The shape of the check was decided by measuring the artefact rather than by reasoning about it**, after a first cut was written against imagined text layers and the spec-auditor caught it: a real NCERT layer glues an inline subscript to its base (`L_p` arrives as `Lp`, `3.84 × 10⁸` as `3.84 × 108`), splits a displayed equation across lines out of reading order, and breaks the odd word with kerning (`the p lanet`). Against that, the token-and-character-count comparison of the first cut flagged **every paragraph carrying a symbol** — it would have buried the founder's ₹430 run in false flags. What replaced it compares squashed text (letters and digits only) and asks the two questions above, neither of which has a budget, a tolerance or a located span to get wrong. **What it cannot see is stated in the class, in the runbook and in its tests**: punctuation is squashed away, so a dropped `×`, a lost leading minus and a dropped prime are invisible — and on this corpus they are invisible *in principle*, because NCERT sets those glyphs in a Symbol font with no Unicode mapping, so `Kepler's` reaches the text layer as `Keplers` and the prime the model stands accused of dropping is not in the layer either. Those are closed by instruction (FIX 2) and by the founder's eye. `PageStructure` is kept ignorant of characters on purpose, but it does **not** cover the shared-source residual — it cannot see a character at all; **only the human step does**, so the audit procedure now requires a few paragraphs read against the *rendered page image* (runbook, D14 ✅ step 4) · when a flagged-to-real ratio is measurable after two books.
- 2026-09-13 · D14 · **Confidence is a review-routing signal, never a guarantee, and the hierarchy is explicit**: the loader's invariants refuse bad data, the mechanical checks find what the invariants cannot see, and confidence only decides what a human looks at first (founder FIX 6; TECH_PLAN §6.3 dated note) · the reasoning-model runs reported high confidence on the two pages that failed the loader, and the tiled cheap-model run reported *zero* low-confidence pages while still carrying three real character defects. A number a model assigns to its own work is worth having and worth never trusting. `ncert extract` also gains a soft per-page paragraph-count flag so that a page the model was sure about can still be listed for a look · never; if confidence proves uncorrelated with real defects after two books, drop it from the report rather than promote it.
- 2026-09-13 · D14 · **Bands only. A whole-page image beside the bands — at any size, in any position — costs every prime, so none is sent; the two-column reading-order case goes to the page-image second read** · four chapter-7 extractions in one evening, ₹55 in all, each measured on the same ten-item glyph table and the same page: bands alone — every prime kept, one two-column worked-example page read out of order; page alone — order right, primes gone; page **with** bands full size first, full size last, and as a 700-px thumbnail too small to read a letter from — order right and every label kept all three times, and **the primes gone all three times**. Position was not the mechanism and legibility was not either: any whole-page view makes the model transcribe from it instead of from the bands it is told to read. A prime the Symbol font never mapped cannot come back from the text layer, and `F'_GB = F_GB` is physics nonsense a student would be taught, so the primes decide it. What bands lose — one mis-ordered two-column page in seventeen — is exactly what a second read of the page image can check, and the prompt now says a left column is read before a right one across every band · when the page-image second read exists and can report whether the two-column case still occurs; if it does not, the layout instruction did the work. ~~The whole page and its bands (superseded within the hour, kept for the measurements):~~ · chapter 7 was extracted both ways on the same evening and each was wrong in exactly one place. Bands alone (the D14 configuration): every prime kept, but ~16 sentences per book cut at the seam and, on a two-column page holding a worked example, a mid-page paragraph placed first. Whole page alone: reading order correct, seam splits gone, ₹1.18 cheaper per chapter — and **the primes vanished** (`F'_GB = F_GB` became `F_GB = F_GB`, audit item 2 back), because the provider shrinks a whole page below the size at which a detached mark survives, and the text layer cannot restore a prime the Symbol font never mapped. Neither is a trade worth making. Three images cost ~₹1.5 a page against ~₹1.1, and the page still carries its own text layer for characters the bands are the fallback for. **Order matters and was measured the same night**: with the page first, the primes vanished exactly as with the page alone (₹15.50 run, 21:32) — the model reads its characters off the first image it is given, whatever it is told about the rest — so the bands went first and the page last — **and the primes vanished again (₹15.51, 21:46), with the reading order and every label intact**. Position was not the mechanism; presence was: given a legible whole page anywhere, the model transcribes from it. So the page now travels as a **700-pixel thumbnail** — the columns, boxes and paragraph breaks are plain at that size and no glyph is legible — which leaves the model nothing to transcribe from but the bands and keeps the layout the page is there for. Third chapter-7 run of the evening decides it · when the page-image second read (`ncert verify --read-pages`, PARKED) exists and can measure whether the bands still earn their third of the bill.
- 2026-09-13 · D14 · **A page-break decision the book prints is code's; one the book only typesets is the model's.** `ncert load` renumbers — and reports — when a page opens at the previous page's address with a printed label (Answer, Solution, Example 4.9), and drops a re-transcribed tail; it no longer refuses on a finished sentence (the row below is superseded, kept for the case that disproved it) · the finished-sentence guard was checked against the six rendered pages it fired on and was **wrong once in three**: Chapter 1 page 4 opens "This shows that the location of decimal point…" flush left after page 3's "…namely four." — one paragraph broken at a full stop, exactly what the guard had assumed cannot happen. The other two were the same real error twice: an Example's question ending page 61 and its **Answer** opening page 62 (Chapter 4), likewise at pages 108–109 (Chapter 6), each numbered as a continuation of the question. Whether a paragraph continues is typography — an indent starts one, flush-left continues one — and invisible to the loader, so that call stays with the model. Whether the page opens with a bold label is a word, so code decides it, and it also decides that everything after the label in that section moves up by one on that page and every later page, because each page's numbering was chained from the one before. The prompt gains the same rule so the next book gets it at source; the loader keeps it so no book depends on the model obeying · **the same evening, on the first load with the repair in place: it fired zero times, because the model had transcribed neither "Answer"** — page 62 opens "As the string is inextensible…" in the stored text and page 109 "The…". A repair keyed on a printed word needs the word to have been transcribed, and nothing in the prompt said so. It does now ("transcribe the printed label as the first words of its paragraph"), which makes the repair reliable from the next extraction; for `phy11-part1` as loaded, the two question-and-answer merges (`ch 4 §4.9.1 ¶13`, `ch 6 §6.7.4 ¶3`) and the numbering after them are **known defects in the canonical rows**, listed in the TRACKER day log, and are cleared by the label rule at the next corpus event rather than by a fourth re-extraction today · when a load's repair section names a label that was transcribed and still mis-numbered — that would mean the model reads the label and continues anyway, and the rule needs the image, not more words.
- 2026-09-13 · D14 · ~~**A page-break join needs a third condition: the first half must not end in a finished sentence**~~ — **superseded the same evening by the row above**, after the rendered pages showed a paragraph running on past a full stop. Kept because the reasoning was sound and the premise was not: · found in the first full book. `ch 4 §4.9.1 ¶18` is a paragraph about rolling friction concatenated with one about circular motion, because page 15 numbered its opening paragraph 18 just as page 14 had, and **both existing conditions were satisfied** — it was that page's first paragraph, and the pages were adjacent. The resulting row changes subject halfway through and repeats a sentence, and a 20-paragraph spot check would have to land on that row to see it. The new condition is not a heuristic dressed up: a paragraph that genuinely runs onto the next page stops where the page's last line stops, which is mid-sentence; one that has finished its sentence has finished. A closing bracket counts as finished, because NCERT ends a displayed equation with its number · never; if a book ever ends a continuing paragraph on a full stop, the refusal names the pages and `--redo` fixes it.
- 2026-09-13 · D14 · **Tiling's cost is measured and paid down in the prompt rather than by turning tiling off**: a band boundary is never a paragraph boundary, and `ncert load` reports every paragraph that begins in the middle of the previous one's sentence · the first full extraction of `phy11-part1` had **32 of 969 paragraphs (3.3%) cut mid-sentence**, always at a printed line end — "The scheme is now for international usage" ended one paragraph and "in scientific, technical and commercial work." began the next. Tiling was adopted at D14 because it fixed 5 of 8 glyph defects, and this is the side of that trade nobody had counted. Turning tiling off would return the glyph defects, which are worse: a fragment is visibly broken to a founder reading the report, while a wrong exponent is invisible until a student is taught it. So the prompt carries the rule and the loader carries the check, free and re-runnable, and the re-extraction is what proves it · when the split count after a re-extraction is measured; if it does not fall, the band overlap (`margai.pipeline.page-tiles`, `PageTiles.OVERLAP`) is the next thing to move.
- 2026-09-13 · D14 · **The transcription conventions fix a notation only where the page cannot supply one; a multiplication sign is transcribed as printed and normalised in code** · the chapter-7 dry run obeyed FIX 2's "scientific notation keeps its sign written as `x`" in 5 paragraphs and wrote `×` in 7, which is the one outcome worse than either choice. The rule was arguing against the prompt's own first principle — transcribe exactly as printed — and losing. The general form, now applied: **invent a convention only for what the page cannot express** (`sqrt`, `approx=`, `i_hat`, Greek by name, all of which replace glyphs that do not survive plain text), and never for a character the page prints perfectly well. Which glyph a multiplication sign is, is exactly what code can normalise, and the ai-layer rule against asking a model for what code can compute applies to *output conventions* and not only to fields · if a downstream consumer ever needs one ASCII form, it normalises then — nothing does today, and a normaliser nothing uses is a guess.
- 2026-09-13 · D14 · **An extraction artefact that a later page shape cannot read is refused by name, with the remedy, not by stack trace** — `ExtractJsonl.read` names the object, the line and the fix · found by the spec-auditor before the founder's run, and it would have stopped that run dead: `has_equations` left the paragraph record, the reader is deliberately strict about unknown properties, and `extract/phy11-part1/en.jsonl` in the bucket is full of lines carrying it — so `ncert extract` would have died on a raw Jackson message while reading the file it resumes from, before a single model call, with no remedy printed. The refusal is also the *correct* answer rather than merely a kinder one: an extraction is canonical per prompt version, so a v2 run must not extend a v1 artefact even if it could parse it · never; the same refusal covers every future shape change, and a truncated line from an interrupted flush gets it too.
- 2026-09-13 · D14 · **The tuning constants of the mechanical checks stay code constants with tests, and do not move to `margai.pipeline.*` config** · `.claude/rules/server.md` sends limits to config, and these look like limits: `MIN_LEGIBILITY`, the structural flag's block size and its two ratios. They are not operational knobs — each is a threshold whose correctness is argued in a test against real page text, and a value changed in SSM would silently bypass every one of those tests while changing what the corpus is checked against. Config is for what ops must change without a deploy; this is algorithm, and it changes with its tests or not at all · if a book ever needs a different threshold from another book, at which point it is data and belongs in `books.yaml`, not in SSM.
- 2026-09-13 · D14 · **The extraction prompt becomes `ncert_extract.v2` and `margai.ai.prompts.ncert_extract.version` moves to 2** (Claude, surfaced with this work order rather than decided silently) · the freeze policy above needs something to name. Every `ncert_paragraphs` row traces to an `ai_calls` row that records the prompt version that produced it, and v1 was amended between eight exploratory runs on one chapter — a corpus frozen against "v1" could not be re-derived, because v1 is not one thing. v2 is the first version whose output is meant to survive · at the next material change to the template after a canonical run exists — which by the policy above is a corpus event, and therefore a v3.
- 2026-09-12 · D14 · **A page PDFBox cannot draw completely fails the run, whatever the format**: `PdfPageRenderer` renders through a `PDFRenderer` whose page drawer rethrows `MissingImageReaderException` from `operatorException` instead of logging it, and `jbig2-imageio` joins `jai-imageio-jpeg2000` on the classpath · the format-specific tripwire added hours earlier did exactly what a format-specific guard does: the next real render cleared JPEG2000 and hit **JBIG2**, reported `result: ok`, exit 0, and wrote 143 pages one of which had a blanked image. A survey by `grep` over the PDFs had said no file used JBIG2 — grep cannot see inside compressed object streams, and trusting it was the mistake behind the mistake. The guarantee now is structural rather than enumerated, and a local sweep renders every page of both pilot books (400+) to prove no gap remains before a paid run · never; the enumerated classpath check stays only as a faster, clearer message.
- 2026-09-12 · D14 · ~~**`PdfPageRenderer` refuses to start without a JPEG2000 reader**~~ — **superseded the same day by the row above** (a format-specific guard missed JBIG2 on the very next run); the dependency and `--redo` stand. Kept for the reasoning: **`jai-imageio-jpeg2000` joins the server's dependencies and `ncert render` gains `--redo`** · found on the founder's first real render: NCERT embeds JPEG2000 images — 21 of the 30 chapter files in the two pilot books — and Java has no decoder for them, so PDFBox logged `Cannot read JPEG2000 image` per page and **rendered the page without the image rather than failing**. The corpus would have degraded silently, and the VISION tier would have read pages whose figures were blank without any signal. Three parts to the fix because the failure had three: the decoder, so pages render whole; a constructor tripwire, so a classpath change can never reintroduce a silent degrade; and `--redo` on render, because the idempotent skip would otherwise preserve every bad page already in the bucket. Verified by rendering real pages of `keph101.pdf` and `kebo110.pdf` and looking at them — the colour mitosis diagrams of Figure 10.2 are there · if a book ever turns up with JBIG2 images, which none of the 30 files uses today (the same tripwire pattern applies).
- 2026-09-12 · D14 · **The end-of-chapter apparatus is found in the PDF's text layer and those pages are never sent to the model** — Summary, Points to Ponder, Exercises, Answers, appendices · asking the model to skip them failed twice, the second attempt making it worse (135 → 149 paragraphs), because NCERT numbers its exercises with the chapter number: Chapter 7's questions run 7.1, 7.2, 7.3, indistinguishable from its sections, and a page of them arriving with no heading reads as a fresh run of sections. A page never shown cannot be misread. **The rule is measured over all 79 English chapter files of all ten books, at the founder's insistence that the pattern be checked across Biology, Physics *and* Chemistry before being chosen** — which is what caught that Chemistry writes `Summary` in title case and gives its exercises no heading at all, and that a Physics chapter's first page lists the apparatus in a contents sidebar. The rule: the first line in the **back half** matching one of those headings case-insensitively. Coverage 79/79, mean position 87%, zero false positives, 17.6% of pages never sent. Only the back half is searched because of that sidebar; a text layer below 0.10 legibility yields no boundary and every page is sent, since skipping a page we cannot read would drop real teaching (`kech202.pdf` is the corpus's one such file, at 0.02 against a 0.26 worst case elsewhere) · D16: **Hindi has no usable text layer at all** (Chanakya glyphs, all ten books), so this detector cannot work there and the Hindi boundary must come from the English edition's structure or another route.
- 2026-09-12 · D14 · **A page missing from the extraction is refused, never assumed blank**: a continuation may cross text-free pages, but every page between the two halves must be *present and empty* · the first version of the gap rule asked only whether any page it could see carried text, so a page absent from the JSONL read as a figure page and the two halves joined silently — and a JSONL with holes is a state the pipeline reaches by design (an interrupted render, a targeted `--redo --pages`, an extract resumed over a different `--chapters`), which is exactly when a founder would be loading (spec-auditor re-audit, D14) · never.
- 2026-09-12 · D14 · **A text-free page carries the address across and drops only the tail, and a paragraph may continue across one** · the first fix for the blocker contained the blocker again: `PreviousPage.of` returned nothing for a page with no paragraphs, so a chapter plate or a full-page figure — the commonest non-prose page, and frequent mid-chapter in Biology — reset the address and the next page restarted at 1. Its other half was a false refusal: requiring strict page adjacency rejected a paragraph legitimately running from page 10 to page 12 across a figure, failing the whole book on correct input. A continuation is now "same address, that page's first paragraph, nothing but text-free pages in between" (spec-auditor re-audit, D14) · never.
- 2026-09-12 · D14 · **A page `--pages` filters out still advances the address when the run knows it** · the remedy the load's refusal prints — `ncert extract --redo --chapters C --pages N` — could never work: the filter skipped the branch that sets the previous address, so a targeted re-extraction always called the model with nothing to continue from, reproducing the collision it was fixing and charging for it (spec-auditor re-audit, D14) · never.
- 2026-09-12 · D14 · **`figure_refs` and `has_equations` are replaced by the edition being loaded, not merged across loads** · merging them looked kinder — the two editions print the same figures — but it made `ncert load` non-idempotent within one edition: a re-extraction correcting a hallucinated `Fig. 7.9` could never clear it, and because the change check compared against the merged value the report called the row *unchanged* while discarding the correction. `--redo` then `load` is the founder's only remedy for a bad page, so the current JSONL has to win outright; provenance stays per edition, which is what the original finding was about (spec-auditor re-audit, D14) · if a Hindi edition is ever found to print figures the English one does not.
- 2026-09-12 · D14 · **An answer cut off at the output-token limit is a non-repairable `InvalidOutputException`** · the repair retry exists to give a model its validation errors and let it try again; a truncation names a config knob the model cannot act on, so the retry re-ran the same page, truncated identically and doubled the cost of the pipeline's most expensive failure (spec-auditor re-audit, D14). `InvalidOutputException` now carries a `repairable` flag, true everywhere else · if a future truncation can be made recoverable by asking for a shorter answer rather than by raising the cap.
- 2026-09-12 · D14 · **The page call carries where the previous page ended — section, last paragraph number and that paragraph's tail — not the tail alone** · spec-auditor BLOCKER on the first cut: the prompt asks for paragraph numbers that restart with a *section* and for a continuation to keep the number it already had, neither of which a model can honour when it is shown only text. It would have restarted at 1 on every page, and the loader's join — which merges anything sharing `(chapter, section, para_no)` — would have concatenated two unrelated paragraphs and dropped the remainder, silently. The loader now joins **only** a true continuation and fails the run by name on any other collision, naming both pages and the `--redo` command that fixes them — *"next page" as first written was superseded the same day by the row above: a continuation may cross text-free pages, and every page in between must be present and empty, because an absent page is a hole the pipeline reaches by design and not a blank one* · never; this is the addressing scheme D14 exists to produce.
- 2026-09-12 · D14 · **`margai.ai.max-output-tokens` is raised to 8,192 and `call-timeout` to 120 s in the `pipeline` profile, and a `max_tokens` stop is now an explicit `InvalidOutputException`** · the 1,024 default sizes a *reasoning answer* inside the 20 s budget (§4.11); a full NCERT body page is 400–900 words plus JSON field names, escapes and figure refs, which reaches or exceeds it on most pages and reliably in Biology — and a truncated tool input still parses, so the failure mode was a silently short page rather than an error (spec-auditor, D14). Checking the stop reason before reading the content turns it into a named refusal that says which knob to turn · when a real page's measured output tokens are known, the cap can be sized rather than guessed.
- 2026-09-12 · D14 · **`ncert load` computes coverage against `ncert_books.pages_*` — the pages `render` produced — not against the pages the JSONL happens to carry; the pages-with-text ratio is reported separately as "text yield"** · dividing by the extraction's own size made an extraction that stopped at page 40 of 240 report ~100% (spec-auditor, D14), and the committed report is the day's evidence. The two numbers answer different questions: coverage asks "was the whole book read", text yield asks "how much of it is prose" — a chapter plate or a full-page figure lowers the second honestly and must not touch the first. A load that is missing rendered pages says INCOMPLETE in its report · never.
- 2026-09-12 · D14 · **The model's `section` and `para_no` are validated before they reach the schema**: a section must be a printed section number, must belong to the chapter the page came from, and must fit `VARCHAR(16)`; `para_no` must be 1–200; text must be non-blank · these are model-supplied strings going into the address a student taps (SPEC §6.3), so "12.4" on a Chapter 7 page is a student sent to the wrong page of a book they are holding, and an over-long section was a raw `DataIntegrityViolationException` at the end of a 260-page load instead of a named reason. The D13 readers apply exactly this strictness to a founder's file (DECISIONS 2026-09-12 D13); a model's output has less claim to trust, not more · never.
- 2026-09-12 · D14 · ~~**`figure_refs` and `has_equations` are merged across editions rather than overwritten**~~ — **the merge half was superseded the same day by the row above** (the re-audit showed it made `ncert load` non-idempotent within one edition); the per-edition `extraction` half stands. Kept for the reasoning: **`ncert_paragraphs.extraction` is keyed by edition — `{"en": {…}, "hi": {…}}`** · a single extraction object let D16's Hindi pass erase the English text's pages, confidence and `ai_call_id`, against the traceability `ParagraphExtraction` promises and against the D14 ✅ itself, which needs a paragraph to name the page it is checked against (spec-auditor, D14). The two editions are read from different images by different calls, so provenance is per edition; the figures a paragraph names and whether it carries an equation describe the paragraph, which is the same paragraph in both editions, so those are unioned · D16, when the Hindi pass runs for real.
- 2026-09-12 · D14 · **The VISION task is `PageExtractTask`, as TECH_PLAN §4.1 names it, behind a new `NcertPageExtractor` port; `ai.api` also gains `AiSpend`** · the first cut shipped `NcertExtractTask`, drifting from the plan's own name for no reason (spec-auditor, D14) — renamed rather than recorded as a deviation, because the plan was right. The port is new: it lets the command that spends be driven end to end without a model, keeping the ArchUnit rule that only `ai.tasks` calls `AiClient`. `AiSpend` is new because §10.5 makes the ledger the source of truth for spend and a run report had no way to read it back · never.
- 2026-09-12 · D14 · **`ncert extract` calls one page at a time and never `completeBatch`, which `.claude/rules/pipeline.md` otherwise requires of pipeline AI calls** · it cannot batch by construction: TECH_PLAN §6.3 specifies "one VISION call per page with the previous page's tail", and since D14 that state includes the previous page's *address*, so each call depends on the answer to the one before it. The 50%-off batch lane (§4.11) is genuinely forfeited on what is for now the largest planned pipeline spend, and that is the price of paragraph numbering that survives a page break · if extraction is ever restructured so a page does not depend on its predecessor — per-chapter batches with a reconciliation pass would be the shape — or at D19–D21, where `pyq solve` has no such dependency and must use the batch lane.
- 2026-09-12 · D14 · **`ncert_books.subject` is its own `BookSubject` enum — physics | chemistry | biology — and not the taxonomy's `Subject`** · NCERT ships one Biology book while the NEET taxonomy splits Biology into botany and zoology (`syllabus_nodes.subject`, V3), so a book cannot carry a taxonomy subject without lying about half of itself; widening `Subject` with a value no `syllabus_node` may ever hold would make that constraint stop saying what it means. A paragraph reaches the botany/zoology split through `node_id` at the D23 anchor pass, which is the level the split actually belongs at · never, unless NCERT splits the Biology book.
- 2026-09-12 · D14 · **`ncert_books.s3_key_en|s3_key_hi` hold the book's source *prefix*, not one object key, and `books.yaml` carries an explicit per-chapter file → chapter-number map** · TECH_PLAN §2.3 names a single key per edition, but NCERT publishes chapter-wise PDFs (`bio11` is `kebo101.pdf … kebo119.pdf` plus prelims): there is no per-book PDF to key. The alternative — concatenating each book into one PDF — invents a derived artefact to be regenerated at every reprint and throws away the chapter boundary the filenames already give. The map also makes the Part-II trap founder-reviewable rather than inferred: `keph201.pdf` is **Chapter 8**, and the printed number is what every anchor means (SPEC §6.3). It makes `chapter_no` deterministic instead of model-guessed · if NCERT ever publishes whole-book PDFs.
- 2026-09-12 · D14 · **Page images are keyed `pages/{book}/{lang}/{chapter}/{page}.png`**, one segment more than TECH_PLAN §6.3 and the F8 row write · a consequence of the row above: sources are per chapter, so PDFBox's page numbers run per chapter too. The alternative, a book-global page number, needs a running offset across twenty PDFs and breaks the moment one chapter's length changes in a reprint · never.
- 2026-09-12 · D14 · **A line of `extract/{book}/{lang}.jsonl` is one page, not one paragraph as §6.3 lists the fields** · three of the fields belong to the page and not to a paragraph — the confidence, the `ai_calls` row that paid for it, and the page number — and a flat row would repeat all three per paragraph; more importantly a page that yields *no* paragraphs (a chapter plate, a full-page figure, an exercise page) must still be recorded as read, or every re-run would pay to extract it again. The per-page line is also exactly one model call, which is what makes the command resumable · never.
- 2026-09-12 · D14 · **A paragraph that straddles a page break is loaded as one row, its halves joined in page order** · the extraction prompt deliberately gives the continuation the paragraph number it already had, so both halves share the address `(book, chapter, section, para_no)`; the loader joins rather than overwrites, records every page in `extraction.pages` and keeps the *lowest* confidence of them. Last-wins would silently drop the first half — a loss a 20-paragraph spot check could easily miss · never.
- 2026-09-12 · D14 · **The `pipeline` profile raises `margai.ai.budget.global-daily-paise` to 200000 (₹2,000/IST day)** · the ₹500 default is a placeholder sized for student traffic, and one pilot book is 150–260 VISION calls: the breaker would have fired mid-book on the first real run, which is a breaker firing on expected use rather than on a loop. The provider console's workspace spend limit (F8, 2026-09-12) is the independent backstop, and the per-user ₹25 breaker is untouched · D65, when the ledger has real pipeline numbers; or the first time a pipeline day legitimately needs more.
- 2026-09-12 · D14 · **The `storage` module is opened with an `ObjectStore` port whose default implementation is in-memory, chosen in one bean method rather than by two conditional configurations** · TECH_PLAN §1.3 designed the module at D3 and D14 is the first day needing a bucket; the default is the implementation that cannot reach AWS, mirroring `FakeAiClient` (DEV_SPEC §13.7), and `describe()` is on the port so a run report always names the store it wrote to. The single bean method replaced a `@ConditionalOnMissingBean` fallback that produced two beans, because across plain `@Configuration` classes that condition depends on registration order — which store a context gets must not · D28, when the uploads bucket, signed URLs and deletion join it.
- 2026-09-12 · D14 · **The D4 `db/seed` test taxonomy will become four real codes from `taxonomy.csv`, keeping its fixed UUIDs; the edit is scheduled for the D18 buffer** · founder ruling 2026-09-12 on the PARKED item that asked for a D14 decision: a local database carrying the seed shows its four invented codes as loader orphans on every taxonomy run, but the fix is taxonomy work and D14 is NCERT work, so deciding now and editing in the buffer keeps one day-scope in flight. Fixed UUIDs stay so `SeedTaxonomyTest` and the constraint tests need no fixture rewrite · D18.

- 2026-09-12 · D13+ · **The per-user breaker stays at ₹25/day: it is a circuit breaker, not a budget** · founder ruling 2026-09-12 on the first measured costs (D5 live re-run): a warm routine doubt is ≈ ₹0.72 and a warm verified numerical ≈ ₹2.50, against ₹2.32 for a *single* cold-cache reasoning call — so ₹25 is 4–7× expected honest usage, which is where a breaker belongs: high enough never to fire on a real student, low enough to stop a runaway loop. It is deliberately **not** sized to the subscription (₹299/month is ₹9.97/day, the annual plan ₹8.22), so a user who consistently maxes it is unprofitable and expected to be rare — TECH_PLAN §7.7 already carries that worst case; the breaker's job is the loop, not the margin · D65's runaway simulation is its acceptance test; revisit if D23's measured per-doubt cost moves the 4–7× multiple materially, or if the price changes.
- 2026-09-12 · D13+ · **A Pro user is never refused: the per-user breaker degrades the way the fair-use cap does — accept, queue, honest copy — and only the free tier keeps a hard stop** · founder ruling 2026-09-12 on a tension the measured numbers exposed: §4.4 promises 30 fresh reasoning solves a day before fair-use queuing while §4.8's ₹25 stops one at ~10, and the two failed *differently* — fair use accepts and queues politely, the breaker returned `AI_BUDGET_EXCEEDED` — which contradicts SPEC §6.3's "nothing is refused for a Pro user", a trust promise rather than a limit. The free tier keeps its hard stop, because there the limit *is* the product boundary. **Refinement surfaced in the same session and adopted:** one policy, two waits, therefore two copies — over the *fair-use* cap the solve stays on §4.4's low-priority on-demand executor and "in a few minutes" remains true; over the *money* breaker it waits for the IST budget reset, so the honest copy says tonight, and the nightly batch lane (00:30 IST, half price, D55) is its natural home precisely because the wait already crosses midnight. Promising "a few minutes" for a wait measured in hours would trade one broken promise for another · D44 (copy and routing), D65 (breaker wiring; the runaway simulation must cover the Pro queue path, not only the trip).
- 2026-09-12 · D13+ · **D23's eval produces the first honest per-doubt cost, and that number triggers a founder re-run of the milestone economics** · today's figures are extrapolated from a smoke prompt with 50-token outputs against a real solver prompt that does not exist until D37, so they are directional at roughly ±40%: the shape (cold vs warm, cheap vs reasoning) is reliable, the absolute rupees are not. They are recorded in §4.8 as measurements of *what was measured* and deliberately **not** propagated into §7.7's monthly projections, which keep their own basis until there is a real number · D23 (founder-pending in the tracker), then D65 against real ledger data (§10.5).
- 2026-09-12 · D13+ · **No throttling layer and no tier upgrade: the cost breaker is the only rate control we build** · the account's limits, read from the provider console on 2026-09-12 (§13.2 item 1), are 10K requests/min, 10M input tokens/min and 2M output tokens/min on each of the two models, with 4K batch submissions/min against a 500K queue — while the ₹500 global daily cap is ≈ $5.60, about **34 seconds** of one minute's input allowance *for an entire day*, and the heaviest planned run (D19–D21, ≈ 2,700 PYQ solutions at concurrency 4 against a 20 s timeout) uses ≈ 0.6% of the output allowance. A throttler sized against a limit three orders of magnitude away would be code that never executes its own branch, and the breaker already refuses the spend that would approach it; the two jittered retries plus the provider's `retry-after` stay as the handling for a 429, which now means a bug rather than saturation · if a second workload joins this organisation (the allowances are org-wide, not per project), if the breaker's caps are raised by more than ~100×, or if a future feature streams high-volume traffic outside the breaker's per-user accounting.
- 2026-09-12 · D13+ · **Secrets stay in SSM Parameter Store as SecureStrings; AWS Secrets Manager is not used** · ECS resolves both through the same task-definition `secrets` field, so the choice costs nothing in code to reverse and the free option wins by default (~11 SecureStrings would be ≈ $4.40/month against §7.7's ₹7,500 fixed infra — real, not decisive); and each thing Secrets Manager sells misses us at beta: **automatic rotation** cannot mint a provider API key, because rotation means calling an API that issues a new credential and neither AI provider exposes one we use — which is also why the runbook's rotation overlap has to come from two keys valid *at the provider* and no staging label could supply it; **cross-account and cross-region replication** needs a second account or region, and we have one of each with staging PARKED (§7.1); and the **64 KB** ceiling is irrelevant while the largest secret is the ≈ 2 KB FCM service-account JSON against the 4 KB standard-parameter limit. This makes explicit what D3.10 chose for "cost and simplicity" without ever naming the alternative — the gap was found when the founder asked, 2026-09-12 · **RDS credential rotation** becoming an audit requirement (D70 hardening or post-launch — the one case where the Lambda integration is genuinely turnkey and "the DB password has not changed in a year" is the finding); a second AWS account or region; or any secret approaching the 4 KB standard limit, the FCM JSON being the one to watch (an advanced parameter at $0.05/month is the smaller step before a store change).
- 2026-09-12 · D13+ · **Model access moves off Amazon Bedrock to the providers' own APIs**: Anthropic for every Claude tier, Cohere for embeddings, selected by `margai.ai.provider` (default `anthropic`); `BedrockAiClient` stays whole but dormant, reachable with `margai.ai.provider=bedrock` **plus `margai.ai.embed.provider` moved with it** (it embeds for itself, and the direct embedding client would otherwise be built and refuse to start without a key it never uses — corrected 2026-09-12 after the spec-auditor read the first wording as "one key") and its price rows supplied by environment; Bedrock is reconsidered after incorporation (F9), parked, not abandoned · this account cannot subscribe to the models on AWS Marketplace — the payment instrument is refused (`INVALID_PAYMENT_INSTRUMENT`, found at the D5 smoke on 2026-09-08) and clearing it needs invoicing against a registered entity, with approval uncertain, so the whole AI layer was blocked behind a company-formation dependency that has no date; direct keys need an account and a card, and every rule the seam enforces (ledger, breaker, tier policy, schema validation, retries) is provider-agnostic, so the switch costs one adapter and no architecture · F9 (incorporation), or if a provider outage argues for a second route.
- 2026-09-12 · D13+ · **SPEC §3's cloud line is amended**: AWS ap-south-1 is named as the infrastructure (RDS, S3, ECS, SSM, SES) and AI models as direct provider APIs, with Bedrock marked optional-later; the parenthesis the line already carried — cheap model for routine work, stronger model for hard problems, batch for nightly, aggressive caching — is unchanged, because none of it depended on the vendor · the contract named the provider, so the switch contradicted it in writing; amended on the founder's explicit per-edit instruction of 2026-09-12 (the D3 ruling's condition), citing the Marketplace block above, and nothing else in SPEC was touched · never, unless the provider changes again.
- 2026-09-12 · D13+ · **The embedding pin is provider + model + dimension together**, `margai.ai.embed.{provider,model,dimensions}` with `send-output-dimension` beside them: Cohere's v4 line at `output_dimension` 1,024, which keeps §2.3's `vector(1024)` columns and needs no migration (none exists yet). **Changing any of the three is a corpus re-embedding and a re-index, never a config flip** — `EmbeddingDimensionTest` holds the config and the migrations' `vector(n)` equal · a stored vector is comparable only to vectors from the same provider, model and width, so the three are one decision and the code must not let them drift apart; the v4 line is multilingual, newer and still 1,024-capable, and switching now is free while switching after D16 is the whole corpus · **rider (founder, 2026-09-12): D17's retrieval harness must include Hindi and Hinglish queries, and if v4 underperforms `embed-multilingual-v3.0` on those, the swap happens before the D16 corpus embedding while it is still free** — two config keys, no code.
- 2026-09-12 · D13+ · **The offline-reasoning lane collapses onto the same reasoning model as the real-time lane, and `batch_min_records` becomes a latency choice rather than a platform floor** · the direct Batches API supports that model (Bedrock's pricing page showed no batch price for it, which was a fact about Bedrock, not the model), takes 50% off every token including cache reads and writes, has **no minimum record count** (ceiling 100,000 requests / 256 MB, results within 24 h), and needs neither the content bucket nor a batch service role — so the lane is cheaper and simpler than the Sonnet 4.6 batch the AWS allowlist had forced on it, and the ≥100 threshold now only expresses "how long may this wait"; the `<100` on-demand path with concurrency 4 is unchanged · D55, when a real batch submission is built — it needs `completeBatch` overridden down the decorator chain, the `batch`/batch-price columns threaded into the ledger, **and the one-record live probe re-added to `AiLiveSmokeTest`** (founder ruling 2026-09-12: the probe was removed the same day because a batch call outside the seam writes no `ai_calls` row and that hard rule admits no test exception, and it comes back once the lane is ledgered rather than staying out); today only the on-demand loop exists, and the lane's support rests on the provider's own reference.
- 2026-09-12 · D13+ · **The independent spend backstop moves from AWS Budgets on a Bedrock line item to a spend limit and alert on the provider's console workspace** (founder sets it, F8 checklist); our own daily alarm stays as planned — computed from the `ai_calls` ledger, provider-agnostic — and the per-user circuit breaker is untouched · the AI bill no longer appears on the AWS invoice, so the backstop has to live where the spend does; two independent limits (theirs, and ours from the ledger) is the same shape as before · D65, when the ledger has real numbers to size both from.
- 2026-09-12 · D13+ · **Per-tier request shape is configuration, not a code branch**: each tier configures `temperature`, `thinking`, `effort` and `cache-min-tokens`, and an absent key means the field is not sent; the reasoning tier ships `thinking: disabled` with `effort: medium` · the models differ in what they *accept*, not merely in what they prefer — the current reasoning model answers a `temperature` with a 400 and the cheap model does the same for `effort` — so "send nothing" had to be expressible, and putting model knowledge in config is the rule the ai-layer already follows; thinking-off keeps a reasoning call inside the 20 s budget and the 1,024-token output cap · **rider (founder, 2026-09-12): D23/D39 compare thinking-on against thinking-off on the hard-numericals subset before beta, and if thinking-on materially improves accuracy, `max-output-tokens` and `call-timeout` are raised then, on that evidence**.
- 2026-09-12 · D13+ · **`AI_LIVE=1` replaces `BEDROCK_LIVE=1` and activates a profile named `live`**, with `margai.ai.provider` choosing the client inside it; no alias is kept, and the dormant provider's smoke selects itself with a `-Dbedrock.smoke=true` system property rather than a second environment variable (the first cut left `BEDROCK_LIVE` able to unlock billable calls on its own — spec-auditor, 2026-09-12) · the switch is "reach a real provider and spend money", which is provider-independent, so naming it after one vendor was what made this rename necessary in the first place; exactly one env var keeps DEV_SPEC §13.7's human-launched guarantee auditable · never.
- 2026-09-12 · D13+ · **The `live` profile without a provider client is a startup refusal, and `margai.ai.provider` is a typed enum** · both provider configurations are `@ConditionalOnProperty` on that value, so a single mistyped character in the deployed parameter matched neither and the chain fell back to `FakeAiClient` *inside the live profile* — the application started normally and would have served students fixtures with no retrieval grounding, no numerical verification and no honest fallback, with one log line as the only sign (spec-auditor, 2026-09-12); the enum stops the typo at binding and the refusal stops any future condition gap, because a fake standing in for a provider is never the right answer · never.
- 2026-09-12 · F8 · **The content bucket is `margai-beta-content` (ap-south-1, SSE-S3, public access blocked, versioned) and the pipeline's prefixes sit at its root**: `source/{ncert,syllabus,pyq}/…` mirroring the founder's local tree, then `pages/{book}/{lang}/{page}.png`, `extract/{book}/{lang}.jsonl`, `snapshots/<date>/`, `batch/in/`, `batch/out/`, `dumps/` · TECH_PLAN §6.2 and §6.3 name the page-image and JSONL keys with no prefix while §7.4 and the 2026-09-09 D12 row write "content/batch/in/" and "S3 `content/`" — read as a nested prefix that would give `margai-beta-content/content/…`, so "content" is taken to mean the content *bucket*, whose name already carries the word; `source/` mirrors the local tree so a key is derivable from a path and the two stay diffable · D14, if `books.yaml` wants a different key shape.
- 2026-09-12 · F8 · **The AWS SDK's `sso` and `ssooidc` modules join `signin` on the server's runtime classpath** so the default credential chain resolves the Identity Center profile `margai` (an `sso_session`, as `aws configure sso` writes it): `sso` exchanges the cached token for role credentials, `ssooidc` refreshes that token; both are loaded reflectively like `signin`, hence `runtime` scope, and the ECS task role needs neither · the D5 `signin` module serves an `aws login` session only, so the first run under the new identity failed with *"To use Sso related properties in the 'margai' profile, the 'sso' service module must be on the class path"* while `aws sts get-caller-identity --profile margai` succeeded — the CLI has its own resolver, the JVM does not; exporting session credentials into the environment (`aws configure export-credentials`) unblocks one shell and would leave every pipeline run from D14 on broken · if the laptop identity ever stops being an Identity Center profile.
- 2026-09-12 · CS-1 · **`momentum_trend` and `strategy_notes` have planner consumers**: momentum scales a node's weightage in candidate priority (`margai.planner.collective.momentum_boost`) and is citable as the PYQ-data claim it is ("NTA has asked this more in recent years"); strategy notes influence block composition and order ("PYQ-first" → practice before learn) and mentor copy — both consumed only from records at or above `min_confidence`, attributed as collective consensus when cited, and never overriding a prerequisite edge or an individual-evidence signal · founder ruling 2026-09-12 on CS-1 open item 3: CS-1 §2 defines the fields and §5 named no consumer; TECH_PLAN §4.5 proposed these and the founder confirmed both with the standard CIL riders · D55, with the synthetic-student checks of CS-1 §7.
- 2026-09-12 · CS-1 · **The D29 first plan reads the approved collective records** — pacing multipliers, priority from `struggle_score`, templated collectively-attributed reasons — **and degrades gracefully** when records are absent or below `min_confidence` (from-pyq-only records or none → a sound plan with default learn minutes and plain weightage-based reasons); the evidence-weighted prior→posterior blend arrives with the nightly re-planner at D55–D56; CS-1 §9.3 amended to name the D29 read · founder ruling 2026-09-12 on CS-1 open item 2: CS-1 §1 puts the two-source read on the day-1 plan while §9.3 mapped it to D55–D56 only — "the best possible plan for a stranger" is made real at onboarding, not in week 10; the `from-inputs` half may slip buffers, so D29 cannot assume a full record set · D29, if the first plan's < 6 s budget is threatened by the read.
- 2026-09-12 · CS-1 · **SPEC §6.1's block-reason sentence now reads "reason drawn from the student's data or, attributed as such, from the collective record (Evidence rule; §9.6)"** · the spec-auditor found the unamended sentence contradicting the Two-sources paragraph beneath it (CS-1 integration audit, MINOR, surfaced as open item 1); amended on the founder's explicit per-edit instruction of 2026-09-12, the D3 ruling's condition · never.
- 2026-09-12 · CS-1 · **The founder's excerpt files under `pipeline/inputs/collective/excerpts/` are ignored by git**; `manifest.md` (names + SHA-256) and `sources.csv` are committed, so a run is reproducible from the manifest without the repo carrying third-party text · CS-1 §3 likens the excerpts to the taxonomy CSV (a committed input) but also forbids storing verbatim quotes and asks for robots/ToS respect — public posts copied into a repository, even a private one, are the one thing CS-1 says the layer never reproduces; the D13 syllabus PDFs set the precedent (ignored, manifest committed); spec-auditor MINOR on the integration · if the repo ever needs the excerpts for a reproducible audit, an S3 `uploads/`-style home with a lifecycle instead.
- 2026-09-12 · CS-1 · **Collective records are one table, `collective_records`, one row per node × season × status (draft | approved)**, the list fields as JSONB, misconception question ids inside the JSONB and validated by `collective load`, no join table (integration decision 4) · the planner reads approved rows of one season only, so a founder review edits drafts without touching what students see; JSONB keeps the CS-1 §2 shape editable in the review sheet; the loader's id check gives the "no unbacked note" guarantee that §6.5's trap evidence table gives by foreign key · D24, when the review sheet's real shape is known; a join table if a query ever has to go from a question to the misconceptions naming it.
- 2026-09-12 · CS-1 · **The prior-to-posterior weighting is a saturating evidence level and a straight line**: `e = 1 − (1 − e_p)(1 − e_d)(1 − e_q)` over practice, diagnostic and doubt evidence on the node, `w = w0 + (w1 − w0) · e` with defaults 0.10 → 0.95, the individual alone above `e_conflict = 0.6`, collective attribution below `w = 0.5`, every constant under `margai.planner.collective.*`, the per-node numbers stored in the plan's snapshot (TECH_PLAN §4.5) · CS-1 §5.2 asks for deterministic, explainable and config-tunable, and a student must be able to read "12 questions on this node → 65% your data" from stored numbers; a Bayesian posterior would be more principled and unexplainable in a reason line · D55 with the synthetic-student checks of CS-1 §7, then the first season's data.
- 2026-09-12 · CS-1 · **SPEC §1 Evidence rule gains one sentence**: a claim about students in general may rest on the collective record (§9.6) and is worded as such; it never stands in for knowledge of this student · forced by consistency — without it §1 and the new §10.9 would contradict each other; the sentence is outside CS-1 §9.1's list (§9, §6.1, §10), so its per-edit authority under the D3 ruling is the founder's approval of decision 1 of the integration plan on 2026-09-12 (TRACKER, CS-1 day log), not CS-1 itself · never.
- 2026-09-12 · CS-1 · **SPEC §10 gains principle 9**: collective claims are attributed as collective, personal claims require personal data, the two are never blended into a false personal claim · forced by CS-1 §9.1 · never.
- 2026-09-12 · CS-1 · **SPEC §6.1 gains the "Two sources" paragraph**: the planner reads the collective record and the student's data, the blend starts collective and shifts to the individual, reasons name their source, honest-ramp copy, the diagnostic named as the fastest shift · forced by CS-1 §9.1 (CS-1 §5) · each season's refresh.
- 2026-09-12 · CS-1 · **SPEC §9 gains item 6, Collective intelligence**: the per-topic collective record as a content asset with its fields, sources, founder review, the CS-1 §3 boundaries as product promises, and the note that the §12 exclusion of cohorts and leaderboards is untouched · forced by CS-1 §9.1 (CS-1 §1–§3) · each season's refresh.
- 2026-09-12 · CS-1 · **The Collective Intelligence Layer is adopted** per the founder's change spec `docs/changes/CS-1-collective-intelligence.md` (issued post-D5, integrated after D13): per-node collective records as a pre-launch content asset; the planner and error diagnosis reading the collective and the individual with a prior-to-posterior blend; four pipeline commands (`collective from-pyq|from-inputs|review|load`); the from-pyq half in the D22–D24 window, `from-inputs` allowed to slip to any later buffer; founder workstream F11 for the excerpt files · a brand-new student's day-1 plan needs a prior before any individual data exists (CS-1 intent), and the Evidence rule is extended, not weakened · each season's `season_version` refresh; CS-1 §8's non-goals if anyone proposes live mining or cohort comparisons.
- 2026-09-12 · D13 · **The readers' numeric bounds are sanity limits, not exam parameters** (year 2000–2100, marks 0–1000) · a validation bound written as "720, the paper's total" would be an exam fact in code; the exam's total and first year belong under `margai.exam.*` (TECH_PLAN §11.5) the day a feature computes with them · D22, `stats compute` is the first computation over marks.
- 2026-09-12 · D13 · **Test infrastructure: the shared Testcontainers Postgres runs with `max_connections=300`** instead of one Spring context reused across the pipeline tests · TECH_PLAN §8.1 prefers context reuse, but the pipeline tests need three contexts by construction — the `pipeline` profile boots without a web server, `importtest` needs an empty schema, `importseed` needs the seed — and every cached context holds a Hikari pool of ten, so the default 100 ran out in the full verify · if the verify passes §8.1's ~3-minute budget, consolidate contexts instead.
- 2026-09-12 · D13 · **The backbone report lists every subject, unit and chapter no step of any track names ("nodes not in any track", TECH_PLAN §6.3) and the tracks the file no longer names**; topics are never named by steps and are left out · the first cut listed only the chapters no track learns, which would have hidden a unit never revised or a subject never mocked (spec-auditor D13) · D55, if the planner starts naming topics in steps.
- 2026-09-12 · D13 · **`pipeline` depends on `common :: api`** (its `IstClock` dates the run report) besides `curriculum :: api`; TECH_PLAN §1.3 listed ai, curriculum and storage for the module and is amended · every module reads the clock through `common`, the omission was §1.3's; §1.4 wants a DECISIONS line per new edge · never.
- 2026-09-12 · D13 · **Every pipeline command writes `pipeline/reports/<yyyy-MM-dd>-<command>.md` (IST day, e.g. `2026-09-12-taxonomy-load.md`) and prints the same markdown**: the run time, the input's absolute path and SHA-256, what was read, the result (`ok` or `FAILED: <reason>`), then the load's counts as tables and its orphan lists; a failed run writes its report too, a missing input (exit 2) writes none; a same-day re-run overwrites its own file · TECH_PLAN §6.3 makes the report the committed evidence of each day's ✅, so it must pin the exact input (the hash) and must exist for a refusal as well; the commands are idempotent, so the last run of a day is the one worth keeping · D14, when reports gain cost lines from the AI ledger.
- 2026-09-12 · D13 · **A track's steps follow the file: `backbone load` upserts by (track, sequence) and deletes the sequences the file no longer has**, the one place a loader deletes; it also enforces the step conventions the schema cannot — a `learn` step names a chapter, `revision` a unit, `mock` a subject — and `cutoffs load` upserts by (year, category, quota_scope, seat_type) with orphans reported, never deleted · a shortened track would otherwise keep ghost steps beyond its last sequence, and steps are owned by their track (no other table references them), unlike nodes and cut-offs · D55, if the planner ever stores per-student copies of steps.
- 2026-09-12 · D13 · **The loaders never delete nodes, prerequisite edges, tracks or cut-offs: rows the file no longer names are reported as orphans and left in place** (`taxonomy load` lists node codes, `taxonomy prerequisites` edges as `FROM -> TO`, `backbone load` tracks, `cutoffs load` natural keys; a track's own steps are the one exception — the row above); the loader enforces what the schema cannot — a parent of the right kind and subject named in the same file, chapter-level prerequisite endpoints that already exist, and Kahn's algorithm over every edge in the database inside the load's transaction, so a cycle rolls the run back with the cycle's members named · `chapter_status` and, later, questions and anchors reference nodes by id, so a delete would cascade into student data or fail on a foreign key; the only orphans today are the D4 test seed's class-scoped shapes (`PHY.11.MECH`, `CHE.11.PHYS`, `PHY.11.KIN`, `CHE.11.MOLE`, three edges) on local and test databases, which the seed test still needs · D14, when the seed's replacement is decided; a `--prune` option if a renamed chapter ever has to go and nothing references it.
- 2026-09-12 · D13 · **The input readers are strict**: a CSV header must match the §6.2 column list exactly, every enum value the V3 CHECK list, codes the `[A-Z0-9]+(\.[A-Z0-9]+)*` pattern of at most 32 characters, and the archetypes YAML refuses unknown keys on a track or a step; a refusal names the file and the CSV line (YAML: the track and the sequence) and fails the run with exit 1 before anything is written; the row records (`SyllabusNodeRow`, `PrerequisiteRow`, `ArchetypeTrackRow`, `ArchetypeStepRow`, `CutoffRow`) live in `curriculum.api` so the pipeline hands them to the import services without touching `curriculum.internal` · a misspelt `target_weeks` silently ignored would ship a step with no week; the founder edits these files by hand (TECH_PLAN §6.2), so the error must be one edit away; Apache Commons CSV 1.11.0 (RFC 4180, the quoted names) and Jackson's YAML module (Jackson 3 line, Boot-managed) do the parsing · if the founder asks for lenient columns (extra columns for notes) — then an explicit allow-list, never a silent ignore.
- 2026-09-12 · D13 · **The security filter chain is a servlet-only bean** (`@ConditionalOnWebApplication(SERVLET)` on `SecurityConfiguration.apiSecurity`); the `JwtDecoder` bean and method security stay unconditional · the `pipeline` profile starts no web server (`spring.main.web-application-type: none`), and there is no `HttpSecurity` to build a chain from where nothing listens; every other bean of the api boots unchanged under the profile, which is the proof that the monolith is one deployable in three shapes (TECH_PLAN §1.2) · if a second non-web profile (`nightly`, D55) needs more than this.
- 2026-09-12 · D13 · **picocli core plus a 15-line Spring `IFactory`, no `picocli-spring-boot-starter`**; leaf commands are Spring beans under the `pipeline` profile, groups and mixins come from picocli's default factory; Spring's own `--spring.*` arguments are filtered before picocli sees the command line; the JVM exits with picocli's code (0 ok, 1 failed, 2 usage or missing input) through an `ExitCodeGenerator` and `MargaiApplication.main` · TECH_PLAN §6.1 names picocli; the starter's Spring Boot 4 compatibility is unproven and the factory is trivial · when the starter publishes a Boot 4 line, if the factory grows.
- 2026-09-12 · D13 · **`--inputs` defaults to `../pipeline/inputs` and `--reports` to `../pipeline/reports`**, relative to the working directory, so `java -jar target/server.jar --spring.profiles.active=pipeline …` run from `server/` finds the repository's `pipeline/` beside it; a missing input file is a usage error (exit 2) naming the resolved path, before anything is written · TECH_PLAN §6.1 puts inputs and reports under the top-level `pipeline/` and the jar is built under `server/`; an absolute default would bind to one laptop · F8, if the pipeline ever runs from the image rather than the founder's laptop.
- 2026-09-11 · D13 · **The 2026 qualifying cut-offs are a historical outlier, and the trajectory feature must not anchor "you clear the cut-off by X" to the latest year alone**: the May 2026 exam was cancelled and the easier Re-NEET of 21 June 2026 inflated the percentile-equivalent marks (general 144 → 213; `cutoffs.csv` rows sourced from the NTA result notice of 16 July 2026, supplied by the founder); D58 uses a smoothed reference — e.g. the median of the last three years — with the latest year shown as context; the 2026 cancellation and re-exam saga is a candidate season note for the Collective Intelligence Layer ("CIL", the founder's term, expanded at the D13 close; the layer is in neither SPEC nor TECH_PLAN today, so it is PARKED, not scope) when its "from-inputs" step runs (still undefined) · a single-year anchor would tell a 2027 aspirant they are 70 marks short of a bar that will not recur (SPEC §9.4 personal target) · D58 (trajectory), and any season whose cut-off moves by more than a normal year's drift.
- 2026-09-10 · D13 · **Archetype step conventions**: a `learn` step names a chapter, a `revision` step an NTA unit, a `mock` step the subject node (four steps per mock week, one per subject); `target_week` is the week by which the step should be reached; inside a week steps run learn → revision → mock with the four subject streams interleaved and each stream in its prerequisite order, a prerequisite inheriting the priority of the chapters that need it and a chapter waiting for a prerequisite that lives in another stream; `default_learn_minutes` is a flat 45 per topic, the chapter the sum of its topics, units and subjects empty; prerequisite edges are chapter-level and cross botany↔zoology where the discipline does (Cell Cycle before Human Reproduction, Genetics before Evolution, Biological Classification before Animal Kingdom — the last added at the founder's review 2026-09-11) · the tables give a step one node and one sequence (TECH_PLAN §2.3) and a mock has no node of its own; NTA units are the natural revision grain; SPEC §9.1 says learn minutes recalibrate from data, so a placeholder suffices; biology is one discipline split administratively into two schema subjects · D55 (the planner spreads a week's steps over days), D22 (real weightage), F3 (the educator review).
- 2026-09-10 · D13 · **`syllabus/*.pdf` is git-ignored with a tracked `syllabus/manifest.md`**, like `ncert/` and `pyq/` · the D12 rule: source PDFs live outside git; the derived, tracked artefact is `pipeline/inputs/taxonomy.csv` · D14, when `ncert register` and `books.yaml` decide the S3 `content/` home for every source PDF (the D12 row deferred that to D13; it moves with the first command that needs the F8 bucket).
- 2026-09-10 · D13 · **Qualifying cut-off rows carry `quota_scope = AIQ`** although NTA's qualifying cut-off is national · stays inside TECH_PLAN §2.3's "AIQ or state code" vocabulary instead of adding an `ALL` value the D22 stats and D25 target code would have to know · D22, when seat-type rows (govt_mbbs, private_mbbs, bds) arrive and carry a real quota scope.
- 2026-09-10 · D13 · **Syllabus units with no NCERT 2022-edition chapter get one synthetic chapter node** with an empty `class_level` and a `00` code segment (`PHY.00.EXPSKILL`, `CHE.00.PBLOCK`, `CHE.00.PRACTICAL`), and NCERT Chemistry Unit 8 is two chapter nodes (`CHE.11.GOC` §8.1–8.7 under NTA Unit 14, `CHE.11.GOCTECH` §8.8–8.10 purification and analysis under NTA Unit 13; section numbers read from the chapter PDF, none in the node names) · topics need a chapter parent (`chapter_status`, anchors) and a chapter has one parent unit; scattering the experiments under theory chapters would lose the NTA unit as a PYQ tagging target · **closed by the founder 2026-09-11, keep all**: NTA reliably asks one or two experimental-skills questions, PBLOCK's two topics match the slimmed p-block content of the current syllabus, the GOC → GOCTECH edge is wired; D23 anchor pass (which paragraphs, if any, anchor the synthetic chapters); D26 syllabus grid (how a class-less chapter renders).
- 2026-09-10 · D13 · **NTA Biology units that span both schema subjects are split into a botany and a zoology unit node** carrying the same NTA number (`BOT.U01`/`ZOO.U01`, U02, U06, U07, U08); Cell Structure and Function (with Biomolecules), Biotechnology and Ecology sit under botany · the V3 CHECK has no `biology` subject and a unit node has exactly one subject; the split follows the prevalent Kota/Allen convention — minority conventions differ on Biomolecules and Biotechnology, which is harmless, since serving and diagnosis are chapter-level and the subject label only affects display grouping and subject-level stats (the founder's note, 2026-09-11) · **closed by the founder 2026-09-11, approved as drafted** (Ecology under botany confirmed: the ORGPOP → ECOSYS → BIODIV chain); F3 educator review only.
- 2026-09-10 · D13 · **Taxonomy tree shape: subject → NTA syllabus unit → NCERT 2022-edition chapter (carries the class level) → topic**; codes `PHY`, `PHY.U06`, `PHY.11.GRAV`, `PHY.11.GRAV.KEPLER`; units carry no class level; the taxonomy follows the NEET (UG) 2026 syllabus, whose content equals 2025's · SPEC §9.1 wants the official syllabus as the tree, while NCERT anchors (§2.3 `ncert_paragraphs`), weightage and `chapter_status` key on NCERT chapters; the D4 seed's `SUBJ.CLASS.ABBR` convention is kept for chapters and topics · D22 / D26 if the unit layer proves unused by the stats or the syllabus grid.
- 2026-09-09 · D12 · **The device-proof driver lives at `scripts/ui.sh`** (tree, tap by label, field, type, backspace, shot, launch, kill, clear; adb, the screenshot directory and the package from the environment), documented in app/README "Device proofs"; a week gate stays commands + a pasted transcript, with the driver as its reusable part — no `scripts/week-gate.sh` · four days of device proofs (D8–D11) rebuilt the same loop in a session scratchpad; the runbook is re-run at every gate and after any `auth`/`account`/`core/api` change, so the loop belongs in the tree, while the gate's shape differs each week · if a third gate repeats the same rows verbatim.
- 2026-09-09 · D12 · **The eval gate's router/routing/retrieval alternative applies under `server/` and `eval/` only, in either case** (`scripts/precommit-gate.sh` `AI_PATHS`: `^(server|eval)/([^/]*/)*[^/]*([Rr]outer|[Rr]outing|[Rr]etriev)[^/]*(/|$)`); `server/**/ai/`, `server/**/prompts/` and `eval/fixtures/` are unchanged · the rule guards the AI difficulty router and retrieval (TECH_PLAN §4.10); the app's go_router under `app/lib/core/router/` tripped it at D8, and the match was case-sensitive, so TECH_PLAN §1.3's `curriculum.api.ParagraphRetrievalRepository` (outside `ai/`, due with D17's retrieval) would never have matched although §1.3 says it does — the spec-auditor caught that the same day, so the case fix landed at D12 rather than D23 · D23, when the stamp becomes the committed `eval/last-pass.json` (TECH_PLAN §0.2); whether router/retrieval *parameters* in config join the hash is PARKED.
- 2026-09-09 · D12 · **`*.pdf` under `ncert/` and `pyq/` is git-ignored**; the two `ncert/2022-ed/{en,hi}/manifest.md` stay tracked · 200 founder-placed PDFs (672 MB) sat untracked in `git status` since 2026-09-09; source PDFs belong in S3 `content/` (TECH_PLAN §6.2) and the pipeline rule's ignored `pipeline/data/` · D13, when the pipeline decides their working home.
- 2026-09-09 · D11 · **The OTP success metric is `first_attempt_rate = otp.verified{first_attempt=true} / otp.sent`** per channel, beside `success_rate = verified / sent`; the report (`auth.api.OtpDeliveryReport`) also carries `send_failed`, `wrong_codes` and `expired_unverified` — codes that reached their expiry unverified, the only "never arrived" signal the server can see until SES bounce/complaint events exist (SNS, F8). Rates are rounded to three decimals and **absent when nothing was sent**, so no zero masquerades as a rate. **The window is the process lifetime** (`since` in the payload): the §10.2 counters are per-process by design (CloudWatch makes one-minute series at F8/D73) and a failed delivery leaves no row (the D7 row below), so one window serves every number rather than a database window mixed with a process counter · SPEC §11 "OTP success ≥ 98% first attempt" names the numerator; DEV_SPEC §9 "OTP delivery rate"; the counters exist since D7 · F8/D73 (CloudWatch windows; the SES event feed).
- 2026-09-09 · D11 · **`otp.failed` carries `channel`; `otp.verified` carries `channel` and `first_attempt` (`true|false`)** — tags, no new meter names, so the §10.4 alarm keeps its names and gains a channel dimension · TECH_PLAN §10.2 listed `otp.failed` without the dimension the other three have, and nothing said whether a match was the first try · never.
- 2026-09-09 · D11 · **The `ops` module opens with `GET /admin/metrics/otp`** (`ops.web.AdminMetricsController`, `@PreAuthorize("hasRole('ADMIN')")`, read-only, one call to `auth.api.OtpMetrics`, no `Principal` parameter because nothing is per-user, every channel listed in enum order); `auth` gains its `api` named interface for it. §3.7's D65/D75 routes join the same module · TECH_PLAN §1.3 lists `ops` and §10.3 already puts a dashboard behind an admin `GET`; the stub needed a home that was not a public `/auth/otp/**` path · D65 (the next route).
- 2026-09-09 · D11 · **`common`'s `ApiExceptionHandler` maps Spring Security's `AccessDeniedException` to the `FORBIDDEN` envelope.** A `@PreAuthorize` refusal throws from inside the controller, past the chain's denied handler, into the advice — where the catch-all logged it as a bug and answered 500 `INTERNAL`; TECH_PLAN §9.3's rule did not work end to end before this. `common` imports one Spring Security exception type: a library, not a module · §3.3 "every exception becomes the envelope"; the first admin route made it visible (D11 plan) · never.
- 2026-09-09 · D11 · **`/actuator/metrics` is exposed and gated on the admin role in the chain** (`/actuator/**` beyond health → `ROLE_ADMIN`, decided before `anyRequest().authenticated()` because actuator endpoints carry no annotation of their own); `/actuator/health` stays public with component names. Amends the D2 row of 2026-09-03 ("only `/actuator/health`") and the D7 row's "admin detail is D75" for metrics only · the founder's raw view of every counter (`auth.refresh.reuse`, `auth.clock_skew`, `ai.calls`) until CloudWatch (F8/D73), at one line of configuration · D75 (any further actuator detail).
- 2026-09-09 · D11 · **The delivery-rate log line is hourly** (`margai.auth.otp.report-every`, `1h`; the same wait before the first line, so boots and tests stay quiet; `1m` for a demo), one INFO line per *enabled* channel on the auth logger with the whole report as `key=value` pairs and `n/a` for an absent rate — never a destination or a code. The reporter schedules itself on the application `TaskScheduler` at `ApplicationReadyEvent` from the bound `AuthProperties.Otp.reportEvery` — one source of truth in Boot's notation, not a `@Scheduled` placeholder with its own ISO-only parser (spec-auditor D11 MINOR). **`@EnableScheduling` lives in `common.internal.SchedulingConfiguration`**, not in a `jobs` module: the `api` task runs its in-process schedules (§1.2, amended) and no feature module owns the platform switch; every profile carries the scheduler thread; each schedule stays with the module whose work it is · PLAN D11 "delivery-rate logging"; TECH_PLAN §10.1 (Logs Insights parses `key=value`), §9.6, §11.5; §1.3 gives `jobs` the *sweepers'* schedules — a metrics heartbeat is observability, not maintenance · D64 (when `jobs` exists, decide whether schedules move) · **the line is plain text locally**: the logstash JSON encoder of §10.1 is not in the tree yet (a D2-era gap) — PARKED for F8/D73, not added at D11.
- 2026-09-09 · D11 · **The DLT half of PLAN D11 was skipped by PLAN's own condition** ("if F1 approved; else stay sandbox" — TRACKER F1 is not started); "sandbox" today means the email path of the D7 ruling, whose SES live proof passed on 2026-09-09 (F10). The MSG91 adapter that the D7 row below names for D11 **moves to the day F1 lands** (TRACKER F1 row) — founder-gated, not a slip. For the ✅ the founder-style admin identity was the account's `users.role` flagged by hand (`update users set role = 'admin'`, §3.7) followed by a fresh login, which mints a JWT with `role = admin`; no admin bootstrap before D75 · PLAN D11 wording; the D7 exit condition · F1 (the adapter), D75 (admin bootstrap).
- 2026-09-09 · D10 · **The `student_profiles` row is created by `AccountService.signIn`**, find-or-create under the per-identifier lock on every login, so an account from before D10 heals on its next sign-in; `GET /me` never writes — an active account without a profile row is empty there and answers `AUTH_INVALID`, and a fresh login repairs it · one code path owns both rows (TECH_PLAN §3.7 "the empty profile row at D10"); a read that writes would hide the repair · never.
- 2026-09-09 · D10 · **A new account starts in the verify call's `Accept-Language`** (the app sends its device locale there; `hi` → Hindi, `hi-Latn` → Hinglish, else English); a later login keeps the stored value; a bare curl stays `en` — amends the D7 row of 2026-09-08 that created the row with `language = en` · SPEC §5 "language auto-suggested, changeable" — a Hindi phone would otherwise flip to English after its first login (TECH_PLAN §5.5 "the chosen value wins on every later start"); the D25 mentor intro still confirms it · D25.
- 2026-09-09 · D10 · **`POST /auth/logout` is authenticated** (TECH_PLAN §1.5 step 3 lists the public routes; logout is not one) and revokes the presented token's family only when that family is the caller's; an unknown token, another user's, or an already revoked family changes nothing — and the answer is 204 either way (naturally idempotent, §3.7; leaks nothing). A spent token still names its family. **The reuse alarm is raised for a rotated-out token only**: a token revoked without a successor (logout, or a family killed by an earlier reuse) is a stale session and answers `AUTH_INVALID` without `auth.refresh.reuse` · §3.2 words reuse as "a rotated refresh token"; a device that logged out and then refreshed once more must not look like an attacker · never.
- 2026-09-09 · D10 · **No bearer is read on the public auth routes** (`BearerTokenResolver` returns nothing on `SecurityConfiguration.PUBLIC_ROUTES`), and the app sends none there · Spring's bearer filter rejects a stale or foreign token even on a `permitAll` route, so an expired access token still in the store would 401 the very `/auth/refresh` meant to replace it · never.
- 2026-09-09 · D10 · **`GET /me` at D10 is `{user, profile}`**; `subscription` (D61), `limits` (D37) and `consent_state` (D27) join as optional fields on their days; `profile` is the scalar columns of `student_profiles` (§2.2) — the JSON documents wait for D28's record shapes · TECH_PLAN §3.1: additive within v1; a field without a producer would be a lie · D27, D37, D61.
- 2026-09-09 · D10 · **`PATCH /me`: absent means unchanged and nothing can be cleared yet**; the body is checked in one pass by `MePayloads.UpdateBody` so every bad field is named at once — `<field>.invalid` for a value outside an enum (the enum is the single source; no regex to keep in step with it), `time.invalid`, and D7's bare constraint names (`not_blank`, `size`, `decimal_min`, `decimal_max`) otherwise; `display_name` trimmed and ≤ 80, hours 0–16 at one decimal, `state_code` two letters upper-cased. The seven new reason codes have no ARB copy yet — the app sends `language` from an enum, so none can reach a screen — and render the D8 fallback line until the screens that can trigger them land (`goal`, `category`, `state_code`, hours at D25; `display_name`, `time` at D64), against the D8 copy row's revisit condition · reason codes, never prose (§3.3); no screen needs to clear a field before D64 · D25, D64 (the copy); D64 (deletion) or the first screen that must clear a field.
- 2026-09-09 · D10 · **A `Principal` parameter on any controller is the caller the chain published** (`common.internal.PrincipalArgumentResolver` over `Principal.REQUEST_ATTRIBUTE`); absent, the answer is the `AUTH_REQUIRED` envelope, never a 500 · every module's controllers read the caller without a dependency on Spring Security or on `auth` (§1.4, §9.3) · never.
- 2026-09-09 · D10 · **After a language switch the app rotates its tokens at once** (`SettingsNotifier.setLanguage`: `PATCH /me` → the stored user rewritten → the locale follows → one `/auth/refresh`); a failed rotation is swallowed — the switch already happened and the next expiry refreshes anyway · §3.8 says the JWT `lang` follows "on next refresh"; making that now keeps `message_user_lang` and the UI in one language from the first call · never.
- 2026-09-09 · D10 · **Logout is best-effort on the server and unconditional on the device**: `POST /auth/logout` with the stored refresh token, and whatever it answers — 204, offline, `AUTH_INVALID` — the store is cleared and the app signs out; no confirmation dialog · SPEC §1 principle 5 (a student on a train can log out; the family expires in ≤ 30 days, §3.2); SPEC §6.11 reserves "clear confirmation" for deletion · never.
- 2026-09-09 · D10 · **Single-flight refresh lives in `SessionRefresher`** (core/auth): one in-flight `/auth/refresh` shared by every caller, the rotated pair stored beside the unchanged user, offline keeps the session for the next attempt. `ApiClient` asks it once for a 401 `AUTH_EXPIRED` **or `AUTH_INVALID`** on an authenticated call — a stored access token signed by a previous server key is `AUTH_INVALID` while the refresh token beside it is still good (the local ephemeral secret, or a rotation past `secret-previous`) — and retries the call once with the new bearer; the session ends (store cleared, `SignedOut`, the D8 guard and `LoginNotifier` reset do the rest) only when the refresh itself answers `AUTH_INVALID` or the retried call is still refused with it, which means the account, not the token · TECH_PLAN §3.3 (`AUTH_INVALID` = re-login) and §5.4, with one repair attempt first so a restart or key rotation never forces a re-login; the client and the refresher are testable alone (spec-auditor D10 MAJOR: the first cut ended the session on the refresh path only) · never.
- 2026-09-09 · D10 · **`localeProvider` follows the account**: the stored user's language while signed in, the device suggestion otherwise; the three language options are written in their own language (English / हिन्दी / Hinglish) in every locale · TECH_PLAN §5.5; a student who picked the wrong language can always find the way back · never.
- 2026-09-09 · D10 · **`/profile` is a full-screen route above the landing**, reached from a person icon on Today's bar until the D29 bottom bar, and offers only the §6.11 items that exist (language, logout) with a note that the rest arrives later · SPEC §1 principle 1: no dead entries · D29 (the shell), D61–D64 (the rest of §6.11).
- 2026-09-09 · D10 · **`meProvider` fetches `/me` once per sign-in and never retries on its own** (Riverpod 3's automatic backoff retry is switched off for it); Today's placeholder watches it, so every start makes the one call of §3.7; screens render from the stored session while it loads or after it fails, with the failure line and one Retry · the D8 discipline — an honest offline state and a manual Retry, no silent background loop · D29 (Today proper).
- 2026-09-09 · D10 · **Retry on Profile and on the landing follows the D8/D9 rule**: offered only after a failure that never produced a server answer — for the switch it re-runs the same language (`SettingsState.lastLanguage`), for a failed `/me` it reloads; a server answer (a reason code, `INTERNAL` with its reference) gets no Retry · one rule for every screen (spec-auditor D10 MINOR: the first cut offered Retry on every `/me` failure) · never.
- 2026-09-09 · D10 · **A flow test may import another module's `internal` types for its test doubles** (`AccountFlowTest` in `account` wires a recording `auth.internal.OtpSender` and decodes with `auth.internal.JwtService`); production code may not, and `ModularityTest` polices main sources only · the alternative is a public test-support surface in `auth.api` nobody else needs · when a second module's flow test needs the same doubles — then a shared test fixture under `com.margai` instead.
- 2026-09-09 · D9 · **`IstClock.now()` truncates to microseconds**, the precision `TIMESTAMPTZ` keeps, so any instant the app writes reads back equal to the one it compares with; test clocks start on a sub-microsecond instant on purpose (`AuthFlowTest.Inbox.CLOCK`) · the PR's first CI run failed two `AuthUnhappyPathsTest` waits by one second (21 for 20, 3508 for 3507): a Linux JDK hands out nanosecond instants, Postgres rounds them up to the next microsecond, and the rounded-up `Retry-After` crossed a second — invisible on macOS, whose instants are microseconds already · if a column ever needs nanoseconds (none in TECH_PLAN §2).
- 2026-09-09 · D9 · **Simultaneous first logins are serialised on a per-identifier advisory lock**: `AccountService.signIn` takes `pg_advisory_xact_lock(hashtextextended('users:' ‖ identifier, 0))` before find-or-create, so two devices verifying a brand-new email at the same instant end as one `users` row with both signed in (one `is_new_user`), instead of the second racing the partial unique index into `INTERNAL` (the D7 known edge); no schema change, no retry loop, no savepoint; pinned at two levels (8 threads on `Accounts.signIn`, two parallel verifies over MockMvc) · the lock is transaction-scoped and costs one round trip on a rare path · if a second API task arrives the lock still works (it lives in Postgres, TECH_PLAN §13.3).
- 2026-09-09 · D9 · **`X-Client-Time` is read on `/api/v1/auth/*` only**, by a servlet filter registered right after the request-id filter (a `FilterRegistrationBean`, so `@WebMvcTest` slices stay unaware of it): a parseable ISO-8601 value puts `client_skew_s` (phone minus server, signed) into the request's MDC; past `margai.auth.clock-skew-warn` (default 2 min) one WARN with the request id and the counter `auth.clock_skew{band=minutes|hours|days}`; absent or unreadable → ignored; never echoed, no `X-Server-Time`, never an input to token validation (server clock, §9.1) · TECH_PLAN §3.1 scoped the header to OTP-flow diagnostics and the app compares durations, not wall clocks (D8), so nothing else needs it · when a non-auth route wants it.
- 2026-09-09 · D9 · **A start with an ephemeral OTP pepper retires every pending challenge** (`expires_at = now` for unverified live rows of any purpose, `OtpStartup` on `ApplicationReadyEvent`), so a code sent before the restart answers one `OTP_EXPIRED` with the resend instead of five `OTP_INVALID`s; a configured pepper leaves codes alone, so a deploy never kills in-flight codes · locally the pepper is per process (D7 DECISIONS) and every restart would otherwise turn a right code into "didn't match"; a production pepper *rotation* has the same ≤ 5-minute effect and is noted in the F8 runbook · if the pepper ever gets a stored key id.
- 2026-09-09 · D9 · **`otp_challenges.created_at` is stamped from `IstClock`**, not Hibernate's `@CreationTimestamp` (VM time): the resend cooldown and the hourly cap compare it with `IstClock.now()`, and TECH_PLAN §11.1 makes that clock the only time source — under a movable test clock the cooldown had silently vanished (200 instead of 429) and the cap's wait drifted · the two clocks agree in production, but a rule that only holds by coincidence is not a rule · never.
- 2026-09-09 · D9 · **The entry step enforces cooldowns client-side, per destination**: after a 429 (or a code sent just before Change email) the Send-code button is disabled with a countdown, `requestCode` is a no-op inside the wait, waits of a minute or more read in whole minutes rounded up on both steps ("Send code in 57 min", "New code in 2 min"), seconds round up too so a blocked button never reads "0s", the once-a-second ticker runs on the entry step only while a cooldown is pending, and typing a different address (trimmed, lowercased) lifts the cooldown — the server keys the cap and the cooldown per destination (§3.4) and still answers 429 if the "new" address is the old one; a 429 `RATE_LIMITED` on **verify** (the per-address verify bucket) shows the server's line and leaves the resend clock alone, since the two limits are different buckets (spec-auditor D9) · found on the AVD: without the different-address rule a student capped on one inbox was stuck 57 minutes on another · never.
- 2026-09-09 · D9 · **Third client-only code `CERTIFICATE`** for a failed TLS handshake or certificate check (dio `badCertificate`, a `TlsException` from the socket), with copy that names the phone's date and time — the usual Android cause — and Retry; Retry is offered after every failure that never produced an envelope (`OFFLINE`, `MALFORMED`, `CERTIFICATE`), still one Retry and no queue (D8) · "the server sent something I couldn't read" was the wrong story for a phone whose clock broke TLS · never.
- 2026-09-09 · D9 · **Request-level reasons render their own line**: a `VALIDATION_FAILED` whose `details` name `body` or `content_type` shows the authored "The app sent something the server couldn't read. Please update the app." instead of the server's generic line, which no field could carry; field reasons keep rendering under their field · the D7 reason codes existed but were never shown · when a further request-level key appears (`FailureCopy.requestLevelKeys`).
- 2026-09-09 · D9 · **The failure checklist is a runbook**, `docs/runbooks/login-failure-checklist.md`: ten rows with trigger, server answer, screen state, pinning tests and the run's evidence, re-run at the Week-2 gate, at D71 and after any change to `auth`, `account`, the app's `core/api` or the login screens; screenshots stay out of the repo, described in the TRACKER day log · PLAN D9's ✅ is a checklist, and a checklist that lives in a day log is not re-runnable · never.
- 2026-09-09 · D8 · **Offline is detected from the failing request itself**: dio connection errors, socket errors and the connect/send/receive timeouts map to `ApiFailure.offline` (client-only code `OFFLINE`); an answer that is not the envelope (a proxy's HTML page, a non-object body, JSON without `error.code`) maps to `MALFORMED` with the status; `connectivity_plus` arrives with the outbox at D34 as TECH_PLAN §5.7 schedules · §5.4 asks for an honest offline state now, and the call that failed already knows — a listener would only add a second opinion · D34, when the outbox needs a connectivity-change event.
- 2026-09-09 · D8 · **Failure copy precedence**: the envelope's `message_user_lang`, then `message_en`, then the app's ARB fallback by error code; reason codes (the `details` of `VALIDATION_FAILED`) and the two client-only codes always come from the ARB; `details` are rendered only for known keys (`attempts_left`, `retry_after_s`, `request_id`, field → reason); the reason-code list lives in `FailureCopy.reasonCodes` and a test walks it in all three locales · TECH_PLAN §3.3 "both sides use the same code strings", §11.7; the server already speaks the caller's `Accept-Language`, so its line is the student's language · the D67 copy pass; whenever the server adds a reason code.
- 2026-09-09 · D8 · **The login flow never drops typed input and never queues**: every failure keeps the email and the code on screen; one Retry re-runs the last intent (the request, or the verify with the same code) and is offered only after a failure that never reached the server; the sixth typed digit submits; a resend keeps the old challenge until the new one arrives, then empties the field; Change email keeps the email and any cooldown; `OTP_EXPIRED` or `attempts_left = 0` disables verify so a new code is the only way on · DEV_SPEC §6 screen 1 "queue + clear errors" read as "no lost input, one retry": a code lives five minutes, so a background queue would replay dead logins · never.
- 2026-09-09 · D8 · **Client plumbing**: `X-Request-Id` is a v4 UUID minted over `Random.secure()` in twelve lines (no `uuid` package); `X-App-Version` is `margai/<version>+<build> android` from `package_info_plus` and becomes the server's `device_label`; `X-Client-Time` is ISO-8601 UTC; `Accept-Language` is the current locale's `en` / `hi` / `hi-Latn` (the device locale until D10/D25 let the student choose); tokens rest in `flutter_secure_storage` (default strong options) as one blob with the user summary — `authStateProvider` reads it once at start to decide who is signed in, the bearer interceptor reads it per call (no in-memory copy to go stale), and a corrupt blob reads as signed out · TECH_PLAN §3.1, §3.8, §5.4, §5.7, §9.1; one blob keeps the three values from disagreeing · D10 adds the single-flight refresh and logout over the same store.
- 2026-09-09 · D8 · **Routing**: the go_router guard is a pure function of auth state and the login step (unknown → splash, signed out → `/login` or `/login/otp` by step, signed in → never a login route), refreshed from both providers, so widgets never navigate; `/today` is a placeholder owned by `planner` (SPEC §8 screen 7's slot: "you're in" plus the identifier the student signed in with, nothing the data cannot back) until D29 · TECH_PLAN §5.3, §5.8; `.claude/rules/app.md` "no logic in widgets"; the guard needs a signed-in landing · D29.
- 2026-09-09 · D8 · **Android build**: `INTERNET` declared in the main manifest (only the debug manifest had it, so a release build could not have signed in); a debug-only `network_security_config.xml` permits cleartext to `10.0.2.2`, `localhost` and `127.0.0.1` while release stays HTTPS-only; `flutter_secure_storage` pinned to 10.x because 11.x compiles against Android 17, which the SDK ships only as the minor-versioned `platforms;android-37.0` that AGP 9.1.0 cannot resolve as `android-37`; the third ARB locale `hi_Latn` exists from D8 with authored Hinglish · TECH_PLAN §5.4 (emulator base URL), §5.5, §9.1; the pin is the boring option over an AGP upgrade on a build day · the pin when the Flutter template's `compileSdk` passes 36; the cleartext file at D74 (release build review).
- 2026-09-08 · D7 · **Login by a verified email while the SMS DLT template (F1) is blocked** — `users.email` joins `phone` as a login identifier (either suffices while active; V6 relaxes the V2 check), `otp_challenges` carry `channel` + `destination`, `POST /auth/otp/request` takes `{phone}` or `{email}`, and the SMS channel stays designed in behind `margai.auth.otp.channels` (default `[email]`); SPEC §3/§5 are untouched and remain the target; TECH_PLAN §1.4, §2.2, §2.10, §3.2, §3.4, §3.7, §7.3, §9.6 carry dated in-place amendments; PLAN D8/D11 and the Week-2 gate read "email" in TRACKER · the founder learned at the D7 plan review that DLT registration needs a registered company; an unverified phone plus an emailed code would let anyone claim another person's number, so email had to be the verified identifier (founder decisions 1 and 3, 2026-09-08) · **exit condition:** F1 approval → add `sms` to the channels, the MSG91 adapter (D11) and a phone-attach-by-OTP flow (PARKED); then this row closes.
- 2026-09-08 · D7 · OTP email delivery through **AWS SES v2 over the SDK default credential chain** (`margai.auth.otp.sender = ses`, `email-from` a verified identity, `ses-region`; the SDK's default retries kept, 15-second call timeout; failures surface as `OtpSendException` carrying only the SES error code); the `log` sender is the sandbox everywhere else · no secrets and the same identity story as Bedrock (TECH_PLAN §7.4); SES needs identity verification and production access (TRACKER F10), not a company registration (founder decision 2) · if SES sandbox limits or deliverability argue for another mailer.
- 2026-09-08 · D7 · `Language` and `UserRole` live in `common.api`, and `common.api.Principal(userId, role, language)` is the request principal every module's controllers read; the security layer publishes it as a request attribute so `common` resolves the response language without depending on Spring Security · common owns the envelope's language and the JWT claims; TECH_PLAN §1.4 gives feature modules no edge to `auth` · never.
- 2026-09-08 · D7 · A blank `margai.auth.jwt.secret` or `margai.auth.otp.pepper` becomes a random 256-bit value for the process with a WARN; a configured value under 256 bits refuses to start; `jwt.secret-previous` is accepted while configured, with no timed window (the F8 rotation runbook removes it after §9.2's 15 minutes) · never a fixed dev key in the tree; local works out of the box and tokens and pending codes die on a restart, the right failure there; SSM (F8) sets both in AWS · F8 SSM seeding, or if a timed window is ever needed.
- 2026-09-08 · D7 · Identifier normalisation: Indian mobiles only at beta (ten digits starting 6–9, optional `+91`/`91`/`0`, spacing tolerated) → E.164; emails trimmed, lowercased, RFC-shape, ≤ 254, no provider canonicalisation (Gmail dots and plus tags PARKED); `code_hash = SHA-256(pepper ‖ challenge_id ‖ code)` hex with a constant-time compare · SPEC is India-only; the challenge id salts every hash · when a non-Indian number is ever a login.
- 2026-09-08 · D7 · OTP outcomes: a request inside the 30-second cooldown or over 3 per hour per destination → 429 `OTP_RATE_LIMITED` with `retry_after_s` (no silent re-send); a phone request while `channels = [email]` → 400 `VALIDATION_FAILED` on `phone`; a wrong code → 401 `OTP_INVALID` with `attempts_left` (0 on the fifth); an unknown, used, expired or exhausted challenge → 401 `OTP_EXPIRED`; a delivery failure after the challenge is stored deletes the row, counts `otp.send_failed` and surfaces as 500 `INTERNAL` with the request id · one remedy per code, the hourly cap stays fair, no new §3.3 codes (closing questions 1, 2 and 4 at the D7 approval) · D9's unhappy-path checklist.
- 2026-09-08 · D7 · The sandbox delivery is `LoggingOtpSender` on its own logger `margai.otp.sandbox` (masked destination plus the rendered message, which carries the code); the auth module's own loggers never see a code or a destination (asserted by `OtpServiceTest`) · the log is the inbox until SES or MSG91 delivers; TECH_PLAN §9.1 "codes never logged" binds the service · never.
- 2026-09-08 · D7 · `POST /auth/otp/verify` creates the `users` row at D7 (`language = en` until the mentor intro or `PATCH /me`; `phone_verified_at` stamped on phone logins); the empty `student_profiles` row and `/me` stay D10; `invite_code` is accepted and ignored until D75; `device_label` = `X-App-Version` (else `User-Agent`, ≤ 80) · the JWT `sub` and the `user` payload need the row; PLAN D10 keeps its scope · D10, D75. *Amended 2026-09-09 (D10, the rows above): the profile row now comes with the users row, and a new account starts in the verify call's `Accept-Language` rather than `en`.*
- 2026-09-08 · D7 · Refresh rotation is sliding: each refresh issues a successor with a fresh 30-day life in the same family; one family per login/device; reuse of a spent token revokes the family, and that revocation commits although the call fails · TECH_PLAN §3.2 "30 days, rotating" read as "a device that keeps being used stays signed in" (PLAN D10 ✅) · never.
- 2026-09-08 · D7 · Rate limiting with Bucket4j (`bucket4j_jdk17-core`) local buckets, greedy refill on the `IstClock`, Caffeine eviction after an hour idle; keys `user:<id>` for authenticated routes, `ip:<address>` for `POST /auth/otp/request` (10/hour) and `ip-auth:<address>` for the other public `/auth/*` routes (`margai.limits.public-auth-per-ip-per-minute`, 60); the client address is the **last** `X-Forwarded-For` hop (the one the ALB appends in its default `append` mode, so a client cannot choose its bucket by prefixing hops), else the socket address; `Retry-After` in whole seconds rounded up, never 0; `/error` is a permitted route so a failure inside a filter still renders the envelope on Boot's ERROR dispatch; the ArchUnit SDK rule is scoped per service package (Bedrock in `ai.internal.bedrock`, SES in `auth.internal.email`) as §1.4 words it · §3.4 names Bucket4j and no eviction library; §1.5 step 4 says "phone/IP for `/auth/*`"; the first-hop key was a spec-auditor MAJOR (bypassable behind an ALB); the earlier ArchUnit rule banned the whole SDK outside bedrock · second API task (§13.3); F8 must keep the ALB's `xff_header_processing.mode` at `append`.
- 2026-09-08 · D7 · Server copy in `messages/messages_{en,hi,hinglish}.properties` for every `ErrorCode` and message kind (a test enforces completeness in all three languages), every line through MessageFormat (double every apostrophe), Hinglish as its own bundle suffix (`Locale.of("hinglish")`) while the app's locale is `hi_Latn`; the envelope's `details` are machine-readable only — ids, seconds, counts and, for `VALIDATION_FAILED`, field → **reason code** (`not_blank`, `min`, `phone.invalid`, `email.invalid`, `identifier.required`, `identifier.one_only`, `channel.unavailable`, `code.digits`, `body: malformed`, `content_type: unsupported`) with snake_case wire names, which the app renders from its ARB copy — a constraint's reason code is its raw message template when that is a code, else the constraint name, never the interpolated (locale-dependent) text; Jackson snake_case, `non_null` output and lenient input in `application.yml`; `/actuator/health` stays public with component names (the D2 row's "revisit at D7": no change, admin detail is D75) · SPEC §3 "all copy is externalized for EN/HI/Hinglish" and TECH_PLAN §3.1 "never text alone" leave no room for English prose in `details` (spec-auditor MAJOR at D7); TECH_PLAN §3.3, §11.3, §11.7 · the D67 copy pass; D8 adds the ARB entries for the reason codes.
- 2026-09-08 · D6 · Developer-machine Bedrock credentials are documented as "the SDK default chain" — an `aws login` session in the default profile (the SDK `signin` module is on the runtime classpath since D5) or a named profile via `AWS_PROFILE` — in `server/README.md`, the `BedrockSmokeTest` and `BedrockConfiguration` javadocs and the pom comment, while TECH_PLAN §1.2, §7.4 and §7.6 keep naming an IAM Identity Center (SSO) profile as the intended identity · the D5 live smoke ran on an `aws login` session (today the account root user, TRACKER F8) and the docs said "SSO profile", which did not describe the working path; the intended identity is F8's to create, not a documentation fact yet (spec-auditor finding, D6) · when F8 creates the non-root identity: the docs then name that profile and this row closes · **closed 2026-09-12**: F8 created the IAM Identity Center profile `margai` and the D5 smoke passed on it (two `ok` rows, forced tool, cache write then read, on `apac.amazon.nova-lite-v1:0` while the Anthropic 403 stands); `server/README.md`, the `BedrockSmokeTest` and `BedrockConfiguration` javadocs and the pom comment now name that profile.
- 2026-09-08 · D5 · `cost_paise` rounds **up** (CEILING), not HALF_UP: any call that consumed tokens costs at least one paisa; a call with no usage stays at zero · the first live smoke (Nova Lite: 29 in / 20 out / 5,976 cached) cost 0.06 and 0.38 paise and rounded to zero, so a stream of tiny calls would never reach the breaker; over-counting by under a paisa per call is the safe direction for SPEC §3's circuit breaker (amends the 2026-09-06 cost row) · if the ledger ever needs sub-paisa precision (schema change: micro-paise).
- 2026-09-08 · D5 · `prices-json` carries a diagnostic row for `apac.amazon.nova-lite-v1:0` (public list prices: 0.06 / 0.24 USD per 1M in / out, cache read 0.006, cache write free, batch at half) so the live smoke can run on an Amazon-owned model while the account's AWS Marketplace subscription for the Anthropic models is blocked (403 INVALID_PAYMENT_INSTRUMENT, AWS ticket open); the tier defaults stay Anthropic and the model is selected per run through `MARGAI_AI_TIER_CHEAP` · proves the Bedrock path — credentials, region, Converse mapping, forced tool, cache accounting, ledger — without touching the contract (SPEC §3 Bedrock) or the tier defaults; a direct Anthropic API fallback was considered and declined for now · when the ticket clears (rerun the smoke on the Anthropic profile; confirm or drop the Nova row).
- 2026-09-06 · D5 · One `ai_calls` row per `AiClient` request: the retry decorator's retries and the schema decorator's repair attempt fold into that row (tokens summed, so the cost is complete; the row reads `ok` when the repair succeeded); the ledger is outermost so breaker refusals and policy errors get rows too; attempts are counted by the `ai.attempts` meter and WARN-logged by the two decorators, and `error_code` stays an error field · TECH_PLAN §4.1 "one AiResponse and one ledger row per request" is how CLAUDE.md's "every Bedrock call logs an `ai_calls` row" is read (spec-auditor finding, D5) · if ops needs per-attempt rows.
- 2026-09-06 · D5 · `AiRequest` carries a ninth component `repair`, set only by the schema-validating decorator; `PromptRef` is name-only — the registry resolves the active version and the ledger stamps it; `Repair`, `AiClientInfo` and `TierPolicyException` live in `ai.api` · the repair turn needs the original request, and a config-driven version cannot live in the caller's reference (TECH_PLAN §4.1 sketch said "name + version") · never.
- 2026-09-06 · D5 · Model-facing fragments shared by every request — the forced tool's description and the repair tool-result message — live in `prompts/_protocol.v<N>.stg`; a `_`-prefixed group file is a fragment group (named templates, no `system`/`user`) rendered through `PromptRegistry.renderFragment` · the ai-layer rule "never inline prompt strings" covers every model-facing string (spec-auditor finding, D5) · never.
- 2026-09-06 · D5 · Output records may declare `Optional<T>` components: optional and nullable in the derived schema, absent or null → `Optional.empty()`; every other component is required. Jackson's missing/null-creator checks are off because the validator enforces required-ness first · the router verdict's subject and node guesses are best guesses (TECH_PLAN §4.2), and a required-everything schema would have rejected them at D37 · never.
- 2026-09-06 · D5 · The breaker refuses a call at spend ≥ cap (the cap is a ceiling: once reached, the next call is refused); the retry count (2), backoff base (500 ms) and on-demand batch concurrency (4) are §4.11 design constants in code, not `margai.*` config · they are resilience policy, not product limits; TECH_PLAN §4.8 says "over budget" without an equality rule · if ops needs to tune them.
- 2026-09-06 · D5 · Embedding input tokens for the ledger come from the `X-Amzn-Bedrock-Input-Token-Count` response header, else the body count (Titan `inputTextTokenCount`), else a ≈ 4-characters-per-token estimate with a WARN — never silently zero · §4.8 "tokens from the response"; a zero would starve the breaker (spec-auditor finding, D5) · D17, when the live embed path runs.
- 2026-09-06 · D5 · Prompt files are StringTemplate 4 **group** files `prompts/<name>.v<N>.stg` with a `system` template (the cached prefix) and a `user` template; `PromptRegistry` loads them at startup, refuses duplicates, and takes the active version from `margai.ai.prompts.<name>.version`, else the highest present · one file per prompt version with the cache boundary explicit; group imports can share a persona later (TECH_PLAN §4.12 said `.st`) · never.
- 2026-09-06 · D5 · `FakeAiClient` ledger rows carry the configured tier model id and realistic usage (≈ 4 characters per token; the system prefix is a simulated cache write on the first call per prompt version and a cache read after) · cost and breaker behave as in production in every profile (DEV_SPEC §13.7 item 6); fake vs live is a profile fact, not a row fact · never.
- 2026-09-06 · D5 · Fake fixtures `ai-fixtures/<prompt>.<case>.json` hold the output object only; `variables.fixture_case` picks a case, otherwise a deterministic hash of the rendered user prompt picks among the public cases; `_`-prefixed cases are failure cases reachable only by name; `<case>.repaired.json` answers the repair retry · the same question always gets the same answer locally, and failure paths cannot leak into ordinary runs · D23, when the eval's fake layer sets cases per fixture id.
- 2026-09-06 · D5 · Structured output: victools jsonschema-generator 5 (Jackson 3 line) derives the tool schema from the output record — snake_case names, every component required, `additionalProperties: false`, enums by constant name — and networknt json-schema-validator 3 validates the model's tool input against that same schema before Jackson 3 decodes it (TECH_PLAN §4.11, D3.21) · one schema object serves Bedrock and validation · if either library fights a Jackson upgrade (the in-house record-schema fallback stays an option).
- 2026-09-06 · D5 · `completeBatch` is the bounded on-demand loop (concurrency 4, virtual threads) as an `AiClient` default method over `this.complete`, so the outermost decorator runs every record through the whole chain; Bedrock batch-job submission and the `batch_inference` flag land with the first ≥ `batch_min_records` caller (D55+) · no caller can reach the minimum before the nightly brain (TECH_PLAN §4.11) · D55.
- 2026-09-06 · D5 · Retries live in `RetryingAiClient` only (two jittered retries on throttling and 5xx); the SDK client runs with `AwsRetryStrategy.doNotRetry()` and `apiCallTimeout` = `margai.ai.call-timeout` (20 s); timeouts are not retried · one retry policy, not two multiplied; §4.11 names throttling and 5xx · never.
- 2026-09-06 · D5 · `RouteDecision` has three factories — `verification()`, `generation()`, `router(RouterVerdict)` — and an ArchUnit rule confines callers of `router(...)` to `ai.tasks.DifficultyRouter` (D37); §4.2's "compile-time property" is an architecture-test property, since Java cannot scope a constructor to one class in another package. A tier-rule breach is a `TierPolicyException` (an `IllegalArgumentException`: caller bug, not a model outcome) · the type keeps REASON honest without a second package · never.
- 2026-09-06 · D5 · `bedrock` profile activation: `MargaiApplication.main` adds the profile when `BEDROCK_LIVE=1`; tests use `@ActiveProfiles("bedrock")` behind `@EnabledIfEnvironmentVariable`; ECS sets `SPRING_PROFILES_ACTIVE`. The bedrock package exposes the live client as an `InnerAiClient` (not itself an `AiClient`) so the one `AiClient` bean stays unambiguous and only that package imports the SDK · TECH_PLAN §1.2 without config tricks; §1.4 import rule holds · never.
- 2026-09-06 · D5 · Cost: `paise = HALF_UP((in·p_in + out·p_out + cr·p_cr + cw·p_cw) / 1e6 × usd_inr × 100)`; batch rows use the batch input/output prices; `input_tokens` excludes cache tokens (Bedrock usage semantics — the smoke rows are the check); the ledger prices an `invalid_output` row for the tokens both attempts consumed, and a `breaker`, `error` or `timeout` row at zero · TECH_PLAN §4.8 at insert time · when Bedrock changes usage semantics.
- 2026-09-06 · D5 · The live smoke is a JUnit test gated by `BEDROCK_LIVE=1` (`BedrockSmokeTest`), two calls on the `cheap` tier, asserting a forced-tool result, real token counts and a prompt-cache read on the second call · TECH_PLAN §13.2 item 1 leaves both proofs to D5; assertions beat eyeballing; PLAN D5 says "one live call" — the second costs under a rupee · never.
- 2026-09-06 · D5 · ArchUnit through the `archunit` core artifact (1.4.2, the version Modulith already brings; no `archunit-junit5`); the "no model-id literal" rule is a source scan of `ai/**` (`ModelIdLiteralTest`) because bytecode offers no string-literal API; family prefixes that select a wire format (`amazon.titan`) are allowed · fewer dependencies, the rule still enforced · never.
- 2026-09-06 · D5 · Local defaults in `application.yml`: `usd_inr = 90`, `global_daily_paise = 50 000` (₹500 per IST day), `max-output-tokens = 1024`, `call-timeout = 20s`; beta values come from SSM (TECH_PLAN §7.3) · placeholders that make the breaker real in `local`; the founder took the recommended figures at the D5 approval · F8 SSM seeding.
- 2026-09-06 · D5 · The 20 `AiFeature` codes and the 5 `AiCallStatus` codes are the CHECK lists of TECH_PLAN §2.8 verbatim, lowercase enum constants (the D4 rule); `Tier` is `cheap|reason|vision|embed` · one spelling in DDL, Java and the ledger · with each new feature (one-line migration plus a constant).
- 2026-09-06 · D5 · `IstClock` lives in `common.api` with one bean (`common.internal.ClockConfiguration`); tests build one over `Clock.fixed` · TECH_PLAN §11.1 "the only way code learns today" · never.
- 2026-09-06 · D4 · `margai.ai.batch_min_records` default stays 100 and `completeBatch` submits with the same `global.` profile IDs as real-time calls (console check #2, TECH_PLAN §13.2 item 2) · the Bedrock quota "Minimum number of records per batch inference job" is 100 for Claude Haiku 4.5 and Sonnet 4.6, not adjustable, maximum 100,000; both models support batch only through cross-region inference profiles, whose region list includes ap-south-1 · if AWS changes the quota, or the beta grows past the minimum and the first real batch job (D55+) surfaces a limit.
- 2026-09-06 · D4 · EMBED = `cohere.embed-multilingual-v3`, on-demand in ap-south-1 itself (no `global.` profile), 1,024 dimensions; `amazon.titan-embed-text-v2:0` at 1,024 dimensions is the fallback by config (console check #4, TECH_PLAN §13.2 item 4) · the plan's first choice is available and proven with an English and a Hindi text; embedding traffic stays in Mumbai; the vector size matches `vector(1024)` without a migration · if retrieval quality on Hindi (D17 ✅) argues for Cohere Embed v4 via its global profile, which would also need an `output_dimension` of 1,024.
- 2026-09-06 · D4 · Ledger price table seeded from the Bedrock Mumbai global cross-region on-demand page (TECH_PLAN §13.2 item 1): Haiku 4.5 1.00 / 5.00, Sonnet 4.6 3.00 / 15.00 USD per 1M in / out, batch at half, cache read 0.10 / 0.30; cache writes billed at the 5-minute rate (1.25 / 3.75) because the plan's cached prefix is per request and 5 minutes is the default `cachePoint` TTL; `usd_inr` is config beside the table · §4.8 computes `cost_paise` at insert from config, never from code, and the table needs one write price per model · at every Bedrock price change, or if a 1-hour cache point is ever used (separate key).
- 2026-09-06 · D4 · Bedrock tier defaults from ap-south-1 (console check #1, TECH_PLAN §13.2 item 1): CHEAP = VISION = `global.anthropic.claude-haiku-4-5-20251001-v1:0`, REASON = `global.anthropic.claude-sonnet-4-6`; current Anthropic models are reachable only through `global.` inference profiles; Sonnet 5, Opus 5 and Opus 4.8 are gated for this account (AccessDenied, AWS Sales allowlist) and go on the F8 list as an upgrade request · founder's Converse probes: the listing shows what exists in the region, only a call proves what the account may invoke; Sonnet 4.6 is the strongest invocable model today · when the allowlist request lands, or the D23 eval shows REASON missing numericals.
- 2026-09-06 · D4 · ArchUnit arrives at D5 with the first import rule it enforces (only `ai` imports the Bedrock SDK); D4 ships Spring Modulith `verify()` alone · nothing to enforce yet · D5.
- 2026-09-06 · D4 · One Testcontainers container per test JVM, kept outside Spring's lifecycle, with one database per set of active profiles plus throwaway databases on demand (`TestcontainersConfiguration`) · Spring stops container beans when any context closes, including one that failed to start; the `test` profile's seed location would fail Flyway validation in a no-profile context sharing the database · never.
- 2026-09-06 · D4 · Seed rows carry fixed UUIDs (`00000000-0000-4000-8000-0000000000NN`) and upsert keyed on that id, so any value including a natural key can be edited and re-applied; edge rows (composite primary key) use `DO NOTHING`; the seed carries no rollback block (the table drops remove it); the three cutoff rows are synthetic and say so in `source` · a repeatable migration re-runs whenever its checksum changes, so it must be idempotent on a persistent local db, and an upsert keyed on `code` would break the moment a code changed (spec-auditor finding, D4) · never.
- 2026-09-06 · D4 · The `local` profile is switched on by `./mvnw spring-boot:run` through the Spring Boot Maven plugin configuration, not by `application.yml`; the packaged jar and tests set profiles explicitly · TECH_PLAN §1.2 "developer default" without a deployed environment ever inheriting the seed location · never.
- 2026-09-06 · D4 · Undo convention: every migration's header carries `-- ROLLBACK:` … `-- END ROLLBACK`, one statement per line (executed line by line); a `rollback/U<n>__*.sql` file takes precedence when present and is handed to the driver whole, so it may contain function bodies; `MigrationReversibilityTest` runs them newest first on a throwaway database and also rejects PostgreSQL enum types · TECH_PLAN §2.9 and §8.2 made executable · never.
- 2026-09-06 · D4 · Prerequisite edge semantics: `from_node_id` must be learned before `to_node_id` (from is a prerequisite of to) · replaces DEV_SPEC §3.2's `prerequisites UUID[]` with a directed join table; the D13 loader follows it · D13.
- 2026-09-06 · D4 · Value types that more than one module stores (`AttemptType` for `student_profiles.attempt_type` and `archetype_tracks.code`, `Category` for `student_profiles.category` and `cutoffs.category`) live in `common.api`; `common` therefore exists from D4 · account and curriculum may not depend on each other (TECH_PLAN §1.4) and two identical enums would drift · never.
- 2026-09-06 · D4 · Enum constants are the lowercase database and wire codes (`Subject.physics`, `AttemptType.fresher_1yr`) with `@Enumerated(EnumType.STRING)` · one spelling in DDL, Java and JSON (TECH_PLAN §2.1, §11.3); no converters · never.
- 2026-09-06 · D4 · Cross-module references are id columns with database foreign keys, never JPA associations (`chapter_status.user_id`, `student_profiles.user_id`); associations are allowed inside one module · a module owns its tables and other modules never read them directly (TECH_PLAN §1.3); the Modulith test would reject the import · never.
- 2026-09-04 · D4 · Database locked: PostgreSQL 18.6 on db.t4g.small, single-AZ, 20 GB gp3 + autoscaling, 7-day backups, deletion protection; pgvector 0.8.1 confirmed via AWS release notes; console create-screen initially hid 18.x — engine/class facts come from the API (`describe-db-engine-versions`, `describe-orderable-db-instance-options`), extension facts from release notes and `pg_available_extensions` · console check #3 (TECH_PLAN §13.2 item 3) closed 2026-09-04; the PG17 fallback (§0.5 item 7) was never applied and is void · minor-version bumps at maintenance windows.
- 2026-09-04 · D3 · Minors: the parent-consent OTP is part of the onboarding flow, and until consent is complete `CONSENT_REQUIRED` covers photo doubts as well as document uploads; text features stay available; after consent everything unlocks (TECH_PLAN D3.28, founder decision 8) · SPEC §6.8 says "before any upload"; a doubt photo is an upload · never.
- 2026-09-04 · D3 · The anonymous peer percentile is shown only when the cohort (same attempt type, active in 14 days) has ≥ 30 students (TECH_PLAN D3.27) · Evidence rule at beta scale · D58.
- 2026-09-04 · D3 · A doubt follow-up weighs 0.5 toward the free-tier limit, like a cached hit (TECH_PLAN D3.26) · SPEC §6.3 is silent; follow-ups reuse context and are usually CHEAP · D44 metrics.
- 2026-09-04 · D3 · Crash reporting through PostHog error tracking, within the three-SDK rule; founder-confirmed at approval, Crashlytics only via an explicit rule amendment (TECH_PLAN D3.25, decision 4) · SPEC §11 needs crash-free % without a fourth SDK · D73.
- 2026-09-04 · D3 · Java records, sealed types, constructor injection; no Lombok (TECH_PLAN D3.24) · Java 25 makes Lombok unnecessary · never.
- 2026-09-04 · D3 · REASON tier reachable only with a `RouteDecision` from three named producers: router, verification, generation (TECH_PLAN D3.23) · the "router only" rule as a type · never.
- 2026-09-04 · D3 · Doubt images deleted immediately after extraction, documents after confirm/discard; the `expires_at` sweeper and the 1-day lifecycle are backstops (TECH_PLAN D3.22) · the 24-hour promise becomes minutes · never.
- 2026-09-04 · D3 · Structured model output via Bedrock Converse forced tool use, validated against the record's JSON schema (TECH_PLAN D3.21) · no JSON parsing heuristics · never.
- 2026-09-04 · D3 · Seed data as a repeatable Flyway migration in a `db/seed` location enabled only in the `local` and `test` profiles (TECH_PLAN D3.20) · production never sees test data · never.
- 2026-09-04 · D3 · `AiClient` = `complete`, `completeBatch`, `embed`; feature tasks around it; ledger, breaker, tier policy, schema validation and retry as decorators (TECH_PLAN D3.18) · stable seam; every rule enforced once · never.
- 2026-09-04 · D3 · Doubt cache keyed by `(question_hash, language)`; a cross-language exact hit renders the verified canonical answer (CHEAP) and counts as a hit (TECH_PLAN D3.17) · keeps the 55% hit-rate target reachable in three languages · D41 metrics.
- 2026-09-04 · D3 · Join tables instead of `UUID[]` where foreign keys matter; plan blocks are rows (TECH_PLAN D3.16) · referential integrity; blocks are addressed by id · never.
- 2026-09-04 · D3 · The nightly run is the same image as a scheduled ECS task (`jobs` module); notifications are scheduled from `PlanGenerated` events and dispatched by the API's in-process scheduler from `notification_log` (TECH_PLAN D3.15) · API latency unaffected; one dispatch mechanism · second API task.
- 2026-09-04 · D3 · Spring Modulith verifies module boundaries in a test from D4 (TECH_PLAN D3.14) · rules a test enforces, not a paragraph · never.
- 2026-09-04 · D3 · Config via `@ConfigurationProperties` records bound from environment variables; `application.yml` holds non-secret local defaults (TECH_PLAN D3.13) · fail-fast validation, no runtime SSM client · never.
- 2026-09-04 · D3 · JWT HS256 15 min + opaque rotating refresh tokens (30 d) with family reuse detection (TECH_PLAN D3.12) · DEV_SPEC §5 made concrete · never.
- 2026-09-04 · D3 · IST for every "today"; `TIMESTAMPTZ` storage; paise integers; UUID v4 keys (TECH_PLAN D3.11) · correctness at the day boundary; boring ids · never (uuidv7 parked).
- 2026-09-04 · D3 · One AWS environment `beta`: Fargate in public subnets behind the ALB (no NAT gateway), RDS single-AZ, secrets injected by ECS from SSM (TECH_PLAN D3.10) · cost and simplicity · public launch.
- 2026-09-04 · D3 · Flutter: go_router, dio, flutter_secure_storage, Riverpod without codegen, drift at D34, flutter_markdown_plus + flutter_math_fork (TECH_PLAN D3.9) · boring, well-supported; codegen only when drift forces build_runner · D34; markdown package health at D32.
- 2026-09-04 · D3 · In-process rate limiting, dispatcher, sweepers and async listeners; single API task (TECH_PLAN D3.8) · beta topology; upgrade path in TECH_PLAN §13.3 · second API task.
- 2026-09-04 · D3 · pgvector HNSW cosine indexes, not ivfflat (TECH_PLAN D3.6) · no training step, better recall at this scale · > 5M vectors.
- 2026-09-04 · D3 · Enumerations as `VARCHAR` + `CHECK`, never PostgreSQL enum types (TECH_PLAN D3.5) · one-line migrations to extend · never.
- 2026-09-04 · D3 · Content pipeline in Java inside the server (`pipeline` profile, picocli), VISION-tier page extraction; `pipeline/inputs/` holds founder data, `pipeline/reports/` the run reports; founder-confirmed at approval (TECH_PLAN D3.4, decision 6) · pipeline.md requires `AiClient`; one toolchain; layout-proof extraction · D14 if extraction quality disappoints.
- 2026-09-04 · D3 · Hinglish is the `hi_Latn` locale (`app_hi_Latn.arb`); server value `hinglish` (TECH_PLAN D3.3) · valid BCP-47 tag, zero custom plumbing; verified with gen-l10n · never.
- 2026-09-04 · D3 · One technical plan document, `docs/TECH_PLAN.md`, cited by section (TECH_PLAN D3.1) · one place for rules and agents to point at · never.
- 2026-09-03 · D2 · One JDK for everything: Flutter's Gradle (9.3.1 / AGP 9.1) builds on the same JDK 25 the server uses, so no `flutter config --jdk-dir` · fewer moving parts in the fresh-clone path · if a Flutter upgrade pins an older Gradle.
- 2026-09-03 · D2 · Actuator exposes only `/actuator/health` with component names, no details · boot proof needs the db component; details wait for auth (D7+) · D7.
- 2026-09-03 · D2 · Android SDK via Homebrew `android-commandlinetools` (no Android Studio); emulator optional through `scripts/dev-setup.sh --emulator` · lighter, scriptable, fits the 15-minute fresh clone · when a contributor needs the IDE.
- 2026-09-03 · D2 · Generated localisations `app/lib/l10n/app_localizations*.dart` are committed · Flutter dropped synthetic l10n packages; CI and IDEs need no extra step · never.
- 2026-09-03 · D2 · Flutter dependencies at D2 are `flutter_riverpod` and `flutter_localizations`/`intl` only; drift arrives with the offline work (D34) · YAGNI · D34.
- 2026-09-03 · D2 · Flyway is wired at D2 with an empty `db/migration` location that carries a README so Flyway's location check passes; the first migration lands at D4 · PLAN D4 owns the first migrations (see the D2 day log for the DEV_SPEC §13.8 wording and how it was resolved) · D4.
- 2026-09-03 · D2 · Server tests use Testcontainers with `pgvector/pgvector:pg18`; the running app uses the compose db · tests never depend on local db state; same image as compose · never.
- 2026-09-03 · D2 · Spring Boot pinned to 4.1.0 on Java 25 from the Homebrew `openjdk@25` formula (no sudo), linked into `~/Library/Java/JavaVirtualMachines` · latest GA of both; CI already pins Java 25 · each Spring Boot minor.
- 2026-09-03 · D2 · Java package `com.margai`; Android applicationId and namespace `com.margai.app` · the specs name no package; three segments is the Android convention · before the first Play upload (D74) or when the final name lands (TRACKER F6/F7) — the applicationId is permanent after upload.
