# Prompt changelog

One line per change to any template under `server/src/main/resources/prompts/*.stg` (prompt
groups and `_`-prefixed fragment groups), model tier routing, or retrieval parameters
(`.claude/rules/ai-layer.md`). Every entry names the eval run that gated it. Newest first.

| Date | Template / area | Change | Eval result | Commit |
|---|---|---|---|---|
| 2026-09-12 | `ncert_extract` v1 (tiled pages) | The user turn gains a block, rendered **only when a page is sent as bands**, telling the model the images are overlapping horizontal slices of one page in reading order, to transcribe each paragraph once and to number across the whole page. Driven by the equation audit: 7 of 35 equation paragraphs carried a small-glyph error (1→l, 1→i, a dropped exponent, a lost minus, dropped primes, `T_M=(1.52)^{3/2}` → `R_M=(1.52)^{1/3}`) while prose scored 98.96% with zero invention — a resolution failure, since the provider shrinks anything over ~1,568px and a 150-DPI page is ~1,754. Whole-page prompts are byte-identical, so the two configurations are directly comparable | placeholder PASS, 0 fixtures — judged instead by re-auditing the same 35 paragraphs of ch 7 |
| 2026-09-12 | `ncert_extract` v1 (figure-label wording) | `figure_refs` now names the printed forms both pilot books actually use rather than only Physics': Biology prints "Figure 10.2 b" and "Figure 10.2 a and b" where Physics prints "Fig. 7.9". Found by rendering a real page of `kebo110.pdf` and reading it — the instruction was already "exactly as printed", but every example was a Physics one, on a corpus that is half Biology | placeholder PASS, 0 fixtures |
| 2026-09-12 | `ncert_extract` v1 (no template change; recorded for the routing it governs) | The prompt is unchanged in this round, but what the call *carries* changed twice on the auditor's findings and the prompt's contract depends on it: a text-free page now hands the running section and paragraph number forward with a null tail, and a `--pages`-filtered page advances the address from the JSONL. Without either, the prompt's "continue from paragraph N" instruction is given no N and the model restarts at 1 | placeholder PASS, 0 fixtures |
| 2026-09-12 | `ncert_extract` v1 (amended before any live run) | **The user turn now carries where the previous page ended — its section and its last paragraph number — not only the tail**, and the system prefix gains "Numbering runs across pages, not within them" with the collision it causes spelled out. The spec-auditor found the first cut unsatisfiable: it demanded numbering that continues within a section across a page break while showing the model only text, so it would have restarted at 1 on every page and collided two paragraphs at one address. Still v1 because nothing had run against it | placeholder PASS, 0 fixtures — the real check is the D14 ✅ and, before it, two consecutive pages of one running section |
| 2026-09-12 | `ncert_extract` v1 (new) | D14 page extraction on the VISION tier (TECH_PLAN §6.1, §6.3): one call per rendered page returns that page's paragraphs at their printed addresses (`section`, `para_no`, `has_equations`, `figure_refs`) plus a confidence, and the previous page's tail travels with the call so a paragraph split across pages is completed, not duplicated. The chapter number is **passed in, never asked of the model** — it comes from the founder-reviewed `books.yaml`, because a Part-II file's printed chapter differs from its file sequence. The system prefix is long by design and by rule (a shipped prompt must clear the cheap model's 4,096-token cache minimum, `AiPropertiesTest`): it carries NCERT page anatomy, the section/paragraph addressing rules, per-subject and per-edition transcription conventions, four worked page examples and a confidence rubric — one book is 150–260 pages that all share it, so it is written once and read at a tenth of input price thereafter. Standing instruction against invention: an illegible gap is marked, never filled | placeholder PASS, 0 fixtures (suite arrives D23) — extraction quality is judged instead by the D14 ✅, 20 random paragraphs read back against the PDFs | d14-ncert-extraction T5 |
| 2026-09-12 | model tier routing + the embedding model | Provider switch (DECISIONS 2026-09-12, TECH_PLAN §4.11): REASON moves to the current Sonnet for both the real-time and the batch lane, CHEAP and VISION to the 4.5 cheap model, all as bare direct-API ids; each tier gains the request shape its model accepts (`temperature`, `thinking`, `effort`) and REASON ships thinking-off at effort medium, so **routing behaviour changes even where the tier does not** — the D23/D39 eval compares thinking-on on hard numericals before beta. The embedding model becomes the v4 line at `output_dimension` 1,024, which **redefines every vector's meaning**: the D17 retrieval harness must cover Hindi and Hinglish, and a swap back to the v3 line happens before the D16 corpus embedding if it underperforms | placeholder PASS, 0 fixtures (suite arrives D23) — the real gate for this cannot run until the suite and a key exist, which is why both riders name a day | 6925a41 (+ the audit fixes) |
| 2026-09-12 | `_protocol` v1, `smoke` v1 | Header comments only, no template text: they named Converse and the Bedrock mapper, and the cache-minimum note now points at `margai.ai.tier.*.cache-min-tokens` instead of a hard-coded figure. Rendered prompts byte-identical | placeholder PASS, 0 fixtures | the audit-fix commit |
| 2026-09-06 | `_protocol` v1 (new) | Shared model-facing fragments for every Converse request: the forced tool's description and the repair tool-result message (moved out of Java on the spec-auditor's finding) | placeholder PASS, 0 fixtures (suite arrives D23) | d5-ai-seam audit fixes |
| 2026-09-06 | `smoke` v1 (new) | D5 connectivity smoke: mentor persona + NEET syllabus outline as a system prefix above the 4,096-token cache minimum, user turn asks for one forced tool call (`SmokeAnswer`) | placeholder PASS, 0 fixtures (suite arrives D23) | d5-ai-seam T2 |
