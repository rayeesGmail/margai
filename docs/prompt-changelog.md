# Prompt changelog

One line per change to any template under `server/src/main/resources/prompts/*.stg` (prompt
groups and `_`-prefixed fragment groups), model tier routing, or retrieval parameters
(`.claude/rules/ai-layer.md`). Every entry names the eval run that gated it. Newest first.

| Date | Template / area | Change | Eval result | Commit |
|---|---|---|---|---|
| 2026-09-14 | `ncert_extract` v3 (amended after the first measurement: no tail, a headings check, one law per paragraph) | **The previous page's ending is no longer quoted** — the user turn names the section and asks for the continuation flag from the page's own typography (a first line mid-sentence, or flush left where the page's paragraphs are indented); the three worked examples that mentioned a quoted tail rewritten. **A headings self-check** in the prefix and the pitfalls: every numbered heading printed on the page must appear as a section change, the paragraph under it transcribed, headings counted against section changes. **A numbered law or rule set as its own paragraph stays one** ("1. Law of orbits", "2. Law of areas", "3. Law of periods"). Evidence: chapter 7 on Haiku echoed the tail on 2 of 12 pages; on Sonnet skipped the "7.3" heading and filed 14 paragraphs under §7.2 and joined law 3 onto law 2 (TRACKER 2026-09-14, DECISIONS). | PASS (placeholder, 0 fixtures); server tests green | this commit |
| 2026-09-14 | `ncert_extract` **v3** (numbering leaves the model; `margai.ai.prompts.ncert_extract.version` → 3) | **The model no longer numbers paragraphs.** It returns each paragraph's section and text and, on the page's first paragraph, `continues_previous_page`; `ncert load` counts ¶n per section across pages. The v2 numbering rules, the "plus one" continuation arithmetic and `previous_para_no` are gone from both templates; the quoted tail shrinks to 200 characters and is named as context for that one flag, "never part of this page's output". New rules from the D14/D15 audits: a printed item marker is part of its paragraph, an equation number keeps its printed parentheses, a unit-opener biography page returns no paragraphs, `P. tigris` and "Figure" as printed. The four worked examples rewritten without numbers. v2 stays on disk as the version phy11-part1's frozen corpus names (DECISIONS 2026-09-14). | PASS (placeholder, 0 fixtures); 576 server tests green | this commit |
| 2026-09-13 | `ncert_extract` v2 (the layer is authoritative for presence, not absence) | **A glyph the image shows and the text layer lacks is real and is transcribed from the image; an equation is never "repaired" to make the layer's version add up.** Found by the D14 ✅ on the canonical rows: `§5.4 ¶5` stored `v_f^2 = 2 × 100 J / 0.05 kg = 63.2 m s^-1` where the page prints v_f = √(…) = 63.2 — the Symbol font drops √ from the layer silently, the model followed the layer's absence of the glyph, then squared the left side so the arithmetic looked right. The shared-source failure predicted in the morning's FIX 1 row, seen once in twenty. The two-sources section gains the sentence; the amendment postdates the canonical run and is applied to that book as a corpus event with the D15 work, after a chapter-7 and a page-5 test | placeholder PASS, 0 fixtures — the check is `ncert extract --redo --chapters 5 --pages 5` on phy11-part1, where the radical must come back |
| 2026-09-13 | `ncert_extract` v2 (bands only, decided) | **The tiled block returns to bands alone**, with one line added: on a two-column page the whole left column is read before the right, across every band. The thumbnail lost the primes too (₹15.50, 21:59) — three configurations with a whole-page image, three times every prime gone, one bands-only run with all of them kept. The primes decide it; the reading-order case is the second read's | placeholder PASS, 0 fixtures — the check is the full-book re-extraction that follows: primes present across the book, and `ch 7 §7.3 ¶11–12` in order or, if not, on the known-defect list for the second read |
| 2026-09-13 | `ncert_extract` v2 (the page as a thumbnail) | Bands first and page last lost the primes exactly as page-first had (₹15.51, 21:46): presence, not position, is what makes the model transcribe from the whole page. The last image is now a 700-px thumbnail, and the block says so — too small to read a letter from, there for the columns, boxes, paragraph breaks and reading order only; every character comes from the bands | placeholder PASS, 0 fixtures — the check is chapter 7 a third time: `F'_GB` present, `§7.3 ¶10–12` in order, labels kept |
| 2026-09-13 | `ncert_extract` v2 (bands first, page last; ≅ as printed) | Two amendments from the ₹15.50 page-plus-bands run on chapter 7. **The whole page moves to the last image and is named as the shrunk one to read no character from**: with it first, every prime vanished exactly as with the page alone — the model transcribes characters from the first image it is given, whatever the words say — while reading order was correct and every label kept. **`≅` and `≈` are written as printed**, `approx=` only as a fallback: the run wrote `g(h) ≅ g(1 − 2h/R_E)`, the sign intact, and by the afternoon's ruling a glyph the page prints is not something to invent a spelling for; `Equations` recognises all three | placeholder PASS, 0 fixtures — the check is chapter 7 once more: `F'_GB` present with the reading order still right |
| 2026-09-13 | `ncert_extract` v2 (the whole page and its bands) | The tiled block now says the first image is the whole page — for columns, boxes, where paragraphs begin and end, and reading order — and the bands after it are the same page at full resolution, for subscripts, primes, minus signs and exponents. Chapter 7 extracted either way alone was wrong in one place each: bands cut sentences at the seam and misordered a two-column page; the whole page alone lost every prime. The model is told which image answers which question rather than left to reconcile three views of one page | placeholder PASS, 0 fixtures — the check is chapter 7 again: primes present *and* `§7.3 ¶11–12` in order, on one run |
| 2026-09-13 | `ncert_extract` v2 (transcribe the label) | **The printed label of a worked example — "Example 7.3", "Answer", "Solution" — is transcribed as the first words of its paragraph, never dropped.** The first load with the loader's label repair in place repaired nothing: on both pages it was built for, the stored text opened "As the string is inextensible…" and "The…" — the model had read past the bold "Answer" and begun at the second word. The repair, and the Answer rule two rows down, both depend on a word being present that nothing had asked for | placeholder PASS, 0 fixtures — the check is the next load's `page-break repairs` section actually naming its repairs |
| 2026-09-13 | `ncert_extract` v2 (the Answer rule) | **A paragraph opening with a printed label — Answer, Solution, Example N.N — is always a new paragraph, never a continuation of the previous page's**, even as the first thing on a page. Two of the three address collisions the first full-book load refused were an Example's question ending one page and its Answer opening the next, numbered as one paragraph (Chapter 4 pp. 61–62, Chapter 6 pp. 108–109, both checked on the rendered image). The loader now repairs this deterministically too, so the corpus does not depend on the model obeying; the rule is here so the next book needs the repair less | placeholder PASS, 0 fixtures — the check is the `page-break repairs` section of the next load, which should name few or none |
| 2026-09-13 | `ncert_extract` v2 (amended on the first full book's evidence) | **A band boundary is never a paragraph boundary**, said in the tiled block with the real example that exposed it, plus **fixed spellings for the marks FIX 2 left out** — `_bar` for an overbar, `_vec` for a vector arrow, `_perp`/`_par`, `sum`, `integral`, `partial`. The first full extraction of `phy11-part1` (143 pages, ₹113) showed 32 of 969 paragraphs beginning mid-sentence, always at a printed line end: the model was closing a paragraph at the bottom edge of a tile band. That is the measured cost of tiling, which was adopted at D14 for fixing 5 of 8 glyph defects and whose price had never been counted. The notation half is the same run's other finding — with no convention given, the model invented `a_bar`, `v_arrow`, `integral`, `sum`, `perp`, so the corpus would have spelled one quantity several ways | placeholder PASS, 0 fixtures — the check is the re-extraction, where `ncert load`'s new split-sentence section must fall from 32 to near zero |
| 2026-09-13 | `ncert_extract` v2 (amended again, before any surviving output) | **The rule fixing scientific notation's multiplication sign as ASCII `x` is deleted; the page's own glyph is transcribed instead.** The chapter-7 dry run wrote `x` in 5 paragraphs and `×` in 7 — the model disobeyed a rule that was arguing against this prompt's own governing principle, "transcribe exactly as printed". Mixed output was the one outcome nobody wanted, and the way to fix it was to remove the rule rather than press harder on it: which glyph a multiplication sign is, is precisely what code can normalise, and `.claude/rules/ai-layer.md` says not to ask a model for what code can compute. `Equations.SCIENTIFIC` now recognises `x`, `×`, `·` and `⋅`. Still v2, on the same test as v1's amendments: the only output produced under the earlier text is the dry run's chapter 7, which is re-extracted before anything else is, so no surviving paragraph traces to it | placeholder PASS, 0 fixtures — the check is the ch-7 re-extraction, where the mixed notation must become consistent |
| 2026-09-13 | `ncert_extract` v2 (amended before any run against it) | **The four surviving instructions to set `has_equations` are removed.** The field had left the tool schema with FIX 3 but stayed in the prefix — in the transcription rules, in the Physics and Biology page descriptions and in a worked example — so the model would have been told four times to emit a property the forced tool's `additionalProperties: false` forbids: a validation failure and a repair retry on every page, billed twice, on a ₹430 run. Found by the spec-auditor, not by the build, because nothing compared the prompt against the schema; `PageExtractTaskTest` now derives the allowed field names from the record and fails if the prefix names one that is not there. Still v2 because nothing had run against it | placeholder PASS, 0 fixtures — the real check is the first page of the re-run, whose `cache write` must show one cached prefix and no repair |
| 2026-09-13 | model tier routing — the `visionsonnet` experiment profile **removed** | The experiment ran and lost, so the profile is deleted rather than left in the tree to rot (founder RULING 1, DECISIONS): 2.2× the cost on the same chapter (₹27.96 against ₹12.71), and **neither reasoning-model run produced a loadable chapter** — one restarted paragraph numbering inside a tile band, the other named `Example 7.1` as a printed section, and the model's own confidence flagged neither. The VISION tier therefore stays on the cheap model for both the content pipeline and student photo doubts, which is also what keeps them one tier | placeholder PASS, 0 fixtures — the comparison was the eight-defect yardstick on ch 7, not the suite |
| 2026-09-13 | `ncert_extract` **v2** (new version; v1 retired) | The page's own **text layer now travels with the image** and is authoritative for characters, the image for layout, reading order and box detection, with the image as the fallback where a span of the layer is garbage — the §6.1 "images, because extractors lose NCERT's layout" reasoning held for structure and was wrong for characters, and every residual defect of the D14 audit (`1` vs `l`, a dropped prime, a lost leading minus, a wrong exponent) is a character these digitally-typeset files already know. With it: the notation gaps the audit exposed are closed (primes preserved, a leading minus is part of the expression, a fixed `i_hat/j_hat/k_hat` convention, `approx=` for ≅/≈ which is never flattened to `=`, `x` kept in scientific notation, subscript and superscript case copied exactly); two split rules (a displayed equation never begins a paragraph; a list item of two or more sentences is its own paragraph); and `has_equations` is **gone from the tool schema** — it is computed in Java from the transcription, because a model should never be asked to judge what code can compute. The version bump is what the canonical-corpus freeze names: v1 ran eight exploratory times on one chapter and was amended between runs | placeholder PASS, 0 fixtures — judged by re-running both pilot books and re-auditing the same sample pages (task 7, founder-run) |
| 2026-09-13 | model tier routing — the `visionsonnet` experiment profile | The VISION tier optionally moves to the reasoning model **for the content pipeline only**, with the request shape that model accepts (no `temperature`, `effort: medium`, 1,024-token cacheable prefix). A separate profile rather than a change to `pipeline`, so the two configurations stay comparable on one chapter and because the same tier serves student photo doubts (SPEC §6.3), which must not change model as a side effect. Prompts are untouched. Testing the residual errors tiling could not fix — `1` versus `l`, dropped primes, a missing leading minus — which are about reading what the page *means* rather than about pixels | placeholder PASS, 0 fixtures — judged on the same eight-defect yardstick as the tiling arm, on ch 7 |
| 2026-09-12 | `ncert_extract` v1 (tiled pages) | The user turn gains a block, rendered **only when a page is sent as bands**, telling the model the images are overlapping horizontal slices of one page in reading order, to transcribe each paragraph once and to number across the whole page. Driven by the equation audit: 7 of 35 equation paragraphs carried a small-glyph error (1→l, 1→i, a dropped exponent, a lost minus, dropped primes, `T_M=(1.52)^{3/2}` → `R_M=(1.52)^{1/3}`) while prose scored 98.96% with zero invention — a resolution failure, since the provider shrinks anything over ~1,568px and a 150-DPI page is ~1,754. Whole-page prompts are byte-identical, so the two configurations are directly comparable | placeholder PASS, 0 fixtures — judged instead by re-auditing the same 35 paragraphs of ch 7 |
| 2026-09-12 | `ncert_extract` v1 (figure-label wording) | `figure_refs` now names the printed forms both pilot books actually use rather than only Physics': Biology prints "Figure 10.2 b" and "Figure 10.2 a and b" where Physics prints "Fig. 7.9". Found by rendering a real page of `kebo110.pdf` and reading it — the instruction was already "exactly as printed", but every example was a Physics one, on a corpus that is half Biology | placeholder PASS, 0 fixtures |
| 2026-09-12 | `ncert_extract` v1 (no template change; recorded for the routing it governs) | The prompt is unchanged in this round, but what the call *carries* changed twice on the auditor's findings and the prompt's contract depends on it: a text-free page now hands the running section and paragraph number forward with a null tail, and a `--pages`-filtered page advances the address from the JSONL. Without either, the prompt's "continue from paragraph N" instruction is given no N and the model restarts at 1 | placeholder PASS, 0 fixtures |
| 2026-09-12 | `ncert_extract` v1 (amended before any live run) | **The user turn now carries where the previous page ended — its section and its last paragraph number — not only the tail**, and the system prefix gains "Numbering runs across pages, not within them" with the collision it causes spelled out. The spec-auditor found the first cut unsatisfiable: it demanded numbering that continues within a section across a page break while showing the model only text, so it would have restarted at 1 on every page and collided two paragraphs at one address. Still v1 because nothing had run against it | placeholder PASS, 0 fixtures — the real check is the D14 ✅ and, before it, two consecutive pages of one running section |
| 2026-09-12 | `ncert_extract` v1 (new) | D14 page extraction on the VISION tier (TECH_PLAN §6.1, §6.3): one call per rendered page returns that page's paragraphs at their printed addresses (`section`, `para_no`, `has_equations`, `figure_refs`) plus a confidence, and the previous page's tail travels with the call so a paragraph split across pages is completed, not duplicated. The chapter number is **passed in, never asked of the model** — it comes from the founder-reviewed `books.yaml`, because a Part-II file's printed chapter differs from its file sequence. The system prefix is long by design and by rule (a shipped prompt must clear the cheap model's 4,096-token cache minimum, `AiPropertiesTest`): it carries NCERT page anatomy, the section/paragraph addressing rules, per-subject and per-edition transcription conventions, four worked page examples and a confidence rubric — one book is 150–260 pages that all share it, so it is written once and read at a tenth of input price thereafter. Standing instruction against invention: an illegible gap is marked, never filled | placeholder PASS, 0 fixtures (suite arrives D23) — extraction quality is judged instead by the D14 ✅, 20 random paragraphs read back against the PDFs | d14-ncert-extraction T5 |
| 2026-09-12 | model tier routing + the embedding model | Provider switch (DECISIONS 2026-09-12, TECH_PLAN §4.11): REASON moves to the current Sonnet for both the real-time and the batch lane, CHEAP and VISION to the 4.5 cheap model, all as bare direct-API ids; each tier gains the request shape its model accepts (`temperature`, `thinking`, `effort`) and REASON ships thinking-off at effort medium, so **routing behaviour changes even where the tier does not** — the D23/D39 eval compares thinking-on on hard numericals before beta. The embedding model becomes the v4 line at `output_dimension` 1,024, which **redefines every vector's meaning**: the D17 retrieval harness must cover Hindi and Hinglish, and a swap back to the v3 line happens before the D16 corpus embedding if it underperforms | placeholder PASS, 0 fixtures (suite arrives D23) — the real gate for this cannot run until the suite and a key exist, which is why both riders name a day | 6925a41 (+ the audit fixes) |
| 2026-09-12 | `_protocol` v1, `smoke` v1 | Header comments only, no template text: they named Converse and the Bedrock mapper, and the cache-minimum note now points at `margai.ai.tier.*.cache-min-tokens` instead of a hard-coded figure. Rendered prompts byte-identical | placeholder PASS, 0 fixtures | the audit-fix commit |
| 2026-09-06 | `_protocol` v1 (new) | Shared model-facing fragments for every Converse request: the forced tool's description and the repair tool-result message (moved out of Java on the spec-auditor's finding) | placeholder PASS, 0 fixtures (suite arrives D23) | d5-ai-seam audit fixes |
| 2026-09-06 | `smoke` v1 (new) | D5 connectivity smoke: mentor persona + NEET syllabus outline as a system prefix above the 4,096-token cache minimum, user turn asks for one forced tool call (`SmokeAnswer`) | placeholder PASS, 0 fixtures (suite arrives D23) | d5-ai-seam T2 |
