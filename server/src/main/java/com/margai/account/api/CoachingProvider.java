package com.margai.account.api;

/** SPEC §5.1 Q3, the "one more tap" for coaching students. Lowercase database and wire codes. */
public enum CoachingProvider {
    pw, aakash, allen, unacademy, other
}
