package com.margai.common.api;

/** {@code users.role} (TECH_PLAN §2.2) and the JWT {@code role} claim; in {@code common.api} since D7 for the {@link Principal}. */
public enum UserRole {
    student, admin
}
